---
milestone: v6.0
name: Production Ready
audited: 2026-03-14T10:00:00Z
status: tech_debt
scores:
  requirements: 14/14
  phases: 16/16
  integration: 4/4
  flows: 4/4
gaps:
  requirements: []
  integration:
    - id: MISSING-03
      description: "CI docker-build job missing VITE_API_KEY build arg — Phase 32 added ARG VITE_API_KEY to frontend/Dockerfile but did not update ci.yml; Docker silently passes empty string, CI-built image has undefined X-API-Key header"
      affected_requirements: [SEC-04, CI-04]
      status: fixed
      fix: "Added VITE_API_KEY=ci-placeholder to .github/workflows/ci.yml build-args alongside existing VITE_CESIUM_ION_TOKEN=ci-placeholder"
  flows:
    - id: BROKEN-03
      description: "CI push → docker-build → frontend production image has non-functional VITE_API_KEY (empty string bundled by Vite)"
      affected_requirements: [CI-04]
      status: fixed
tech_debt:
  - phase: 27-secrets-cleanup
    items:
      - "ADVISORY: Real credentials (OpenSky client secret, AISStream API key) exist in git history from pre-Phase-27 commits. User must rotate both keys before making repository public. Optionally purge history with git filter-repo."
  - phase: 30-ci-pipeline
    items:
      - "SUMMARY.md frontmatter missing requirements_completed field — CI-01 through CI-04 are verified SATISFIED in VERIFICATION.md requirements table but not listed in SUMMARY YAML. Minor documentation gap."
  - phase: all-v6.0
    items:
      - "Nyquist VALIDATION.md files exist for all 6 v6.0 phases (27-32) but all have nyquist_compliant: false with status: draft. Run /gsd:validate-phase for each to close."
      - "Human verifications pending: Phase 27 (docker compose config error without .env), Phase 30 (GitHub Actions live green run), Phase 31 (README prose review), Phase 32 (full stack smoke test with POST /api/osint-events)."
      - ".env.example line 26 has stale VITE_API_BASE_URL=http://localhost:3000 (dev value, unused in production — misleading for operators)."
      - "README.md documents FRONTEND_ORIGIN default as http://localhost:3000 (dev value); production operators need http://localhost."
      - "LICENSE <IWMO> author placeholder — confirm owner name before public release."
nyquist:
  compliant_phases: []
  partial_phases: [27, 28, 29, 30, 31, 32]
  missing_phases: []
  overall: partial
---

# v6.0 Production Ready — Milestone Audit

**Audited:** 2026-03-14
**Status:** tech_debt — all 14 requirements satisfied, no critical blockers, accumulated items need review
**Milestone Goal:** Harden the project for public release — scrub secrets, build a production-grade Docker stack with nginx reverse proxy, add a full GitHub Actions CI pipeline, and ship a README + LICENSE.

---

## Requirements Coverage: 14/14

### 3-Source Cross-Reference

| REQ-ID | Description | Phase | REQUIREMENTS.md | SUMMARY Frontmatter | VERIFICATION.md | Final Status |
|--------|-------------|-------|-----------------|---------------------|-----------------|--------------|
| SEC-01 | No hardcoded credential fallbacks in docker-compose.yml | 27 | [x] | listed | passed | **satisfied** |
| SEC-02 | backend/.dockerignore and frontend/.dockerignore exclude .env | 27 | [x] | listed | passed | **satisfied** |
| SEC-03 | .env.example includes all required variables with placeholders | 27 | [x] | listed | passed | **satisfied** |
| SEC-04 | API key middleware on POST /api/osint; key forwarded; UI sends X-API-Key | 28+32 | [x] | listed (both phases) | passed (both phases) | **satisfied** |
| PROD-01 | docker-compose.yml frontend uses production build target | 29 | [x] | listed | passed | **satisfied** |
| PROD-02 | nginx config routes /api/* to backend container | 29 | [x] | listed | passed | **satisfied** |
| PROD-03 | Single public entry point on port 80 via nginx | 29 | [x] | listed | passed | **satisfied** |
| PROD-04 | Docker Compose healthchecks on backend, worker, ais-worker | 29 | [x] | listed | passed | **satisfied** |
| CI-01 | GitHub Actions runs pytest on every push and PR | 30 | [x] | **missing** | human_needed (SATISFIED in req table) | **satisfied** |
| CI-02 | GitHub Actions runs vitest run + tsc --noEmit | 30 | [x] | **missing** | human_needed (SATISFIED in req table) | **satisfied** |
| CI-03 | GitHub Actions runs gitleaks secret scanning | 30 | [x] | **missing** | human_needed (SATISFIED in req table) | **satisfied** |
| CI-04 | GitHub Actions verifies Docker images build successfully | 30 | [x] | **missing** | human_needed (SATISFIED in req table) | **satisfied** |
| DOC-01 | Root README.md covers setup, Docker Compose, API key config | 31 | [x] | listed | passed | **satisfied** |
| DOC-02 | LICENSE file added to repository | 31 | [x] | listed | passed | **satisfied** |

**Notes:**
- CI-01..04 classified satisfied despite SUMMARY frontmatter gap: VERIFICATION.md explicitly marks all four SATISFIED; human_needed status reflects GitHub Actions runner limitation (cannot run locally), not a logic gap.
- SEC-04 covered by two phases: Phase 28 (backend middleware + route guard) and Phase 32 (Dockerfile ARG, compose build arg, frontend header). Both required for full chain compliance.

### Orphan Detection

No orphaned requirements found. All 14 REQ-IDs in the REQUIREMENTS.md traceability table appear in at least one VERIFICATION.md requirements table.

---

## Phase Verification Summary: 16/16

| Phase | Name | Status | Score | Human Verification Pending |
|-------|------|--------|-------|---------------------------|
| 27 | Secrets Cleanup | human_needed | 3/3 | docker compose config error without .env |
| 28 | API Key Auth | passed | 4/4 | — |
| 29 | Production Docker Stack | passed | 4/4 | — |
| 30 | CI Pipeline | human_needed | 5/5 | GitHub Actions live run (4 jobs green) |
| 31 | Documentation | human_needed | 8/8 | README prose review |
| 32 | API Key Wiring | human_needed | 6/6 | Full docker compose stack smoke test |

---

## Cross-Phase Integration: 4/4 Flows Verified

Integration verified by direct source inspection.

### Flow 1: Operator Onboarding

`cp .env.example .env` → fill values → `docker compose up` → `http://localhost`

| Step | Check | Status | Evidence |
|------|-------|--------|----------|
| .env.example present with all vars | Phase 27 | WIRED | All 6 required vars present with placeholders |
| Compose fails without .env | Phase 27 | WIRED | Bare `${VAR:?Set VAR in .env}` — mandatory error if unset |
| nginx serves on port 80 | Phase 29 | WIRED | `ports: "80:80"` on frontend service; no Vite dev port exposed |
| nginx proxies /api/* to backend | Phase 29 | WIRED | `nginx.conf`: `location /api/ { proxy_pass http://backend:8000; }` |

### Flow 2: Authenticated Write

UI POST `/api/osint-events` with `X-API-Key` → backend validates → 201/401

| Step | Check | Status | Evidence |
|------|-------|--------|----------|
| VITE_API_KEY baked into frontend at build time | Phase 32 | WIRED | `frontend/Dockerfile` lines 14-15: `ARG VITE_API_KEY`, `ENV VITE_API_KEY=$VITE_API_KEY` |
| Compose passes VITE_API_KEY as build arg | Phase 32 | WIRED | `docker-compose.yml`: `VITE_API_KEY: ${VITE_API_KEY:?}` in `build.args` |
| UI sends X-API-Key header | Phase 32 | WIRED | `OsintEventPanel.tsx` line 52: `'X-API-Key': import.meta.env.VITE_API_KEY` |
| nginx proxies POST /api/* to backend | Phase 29 | WIRED | `proxy_pass http://backend:8000` on `/api/` |
| deps.py validates header | Phase 28 | WIRED | `verify_api_key` checks `x_api_key != settings.api_key` → raises 401 |
| Route protected | Phase 28 | WIRED | `routes_osint.py`: `dependencies=[Depends(verify_api_key)]` on POST |
| API_KEY forwarded to backend container | Phase 32 | WIRED | `docker-compose.yml`: `API_KEY: ${API_KEY:?}` in backend env |

**Full chain:** `.env VITE_API_KEY` → build arg → Vite inline → JS fetch header → nginx proxy → FastAPI guard → 201 or 401

### Flow 3: CI on Push/PR

`git push` → GitHub Actions triggers → 4 parallel jobs run

| Job | Status | Evidence |
|-----|--------|---------|
| pytest (postgis service container) | WIRED | `jobs.pytest` with postgres service, alembic migration, API_KEY env |
| vitest + tsc --noEmit | WIRED | `jobs.frontend` with `npx tsc -p tsconfig.app.json --noEmit`, `npx vitest run` |
| gitleaks secret scan (full history) | WIRED | `gitleaks/gitleaks-action@v2`, `fetch-depth: 0`, `.gitleaks.toml` with 7 SHA allowlist |
| docker build --target production | WIRED | Two `docker/build-push-action@v6` steps with `target: production`, `push: false` |

### Flow 4: Secrets Isolation

No credentials leak into Docker images or CI logs

| Check | Status | Evidence |
|-------|--------|---------|
| .dockerignore excludes .env files | WIRED | Phase 27: both `.dockerignore` files exclude `.env`, `*.env` |
| Compose uses bare ${VAR:?} (no :-defaults) | WIRED | No `:-default` fallbacks for any secret variable |
| CI uses placeholder build arg | WIRED | `VITE_CESIUM_ION_TOKEN=ci-placeholder` (intentional, not a secret) |
| gitleaks scans full history | WIRED | `fetch-depth: 0` on secret-scan checkout |

---

## Tech Debt by Phase

### Phase 27: Secrets Cleanup

**1. Credential rotation required before public release**

Real credentials (OpenSky OAuth2 client secret, AISStream API key) exist in git history from pre-Phase-27 commits. Phase 27 removed hardcoded values from the living codebase but did not purge history.

**Action required:**
1. Revoke OpenSky client secret at https://opensky-network.org/
2. Revoke AISStream API key at https://aisstream.io/
3. Optionally purge history: `git filter-repo --path docker-compose.yml --invert-paths`

*This is a user action item — not a code defect.*

---

### Phase 30: CI Pipeline

**2. SUMMARY.md frontmatter missing requirements_completed**

`30-01-SUMMARY.md` `requirements_completed` field is empty despite CI-01..CI-04 all being SATISFIED in the VERIFICATION.md requirements table. Minor documentation inconsistency.

---

### All v6.0 Phases

**3. Nyquist VALIDATION.md files incomplete**

| Phase | VALIDATION.md | nyquist_compliant | Action |
|-------|--------------|-------------------|--------|
| 27 | exists | false (draft) | `/gsd:validate-phase 27` |
| 28 | exists | false (draft) | `/gsd:validate-phase 28` |
| 29 | exists | false (draft) | `/gsd:validate-phase 29` |
| 30 | exists | false (draft) | `/gsd:validate-phase 30` |
| 31 | exists | false (draft) | `/gsd:validate-phase 31` |
| 32 | exists | false (draft) | `/gsd:validate-phase 32` |

**4. Human verifications pending**

| Phase | Pending Verification |
|-------|---------------------|
| 27 | `docker compose config` produces hard error when `.env` absent |
| 30 | All 4 GitHub Actions jobs appear and turn green on a real push |
| 31 | README top-to-bottom review — Quick Start steps actionable without other files |
| 32 | Full stack smoke test: POST without key → 401; with key → 201; UI LOG EVENT button works |

---

## Nyquist Compliance Summary

| Status | Phases |
|--------|--------|
| COMPLIANT | none |
| PARTIAL (draft) | 27, 28, 29, 30, 31, 32 |
| MISSING | none |

**Overall: PARTIAL** — VALIDATION.md files exist but none finalized. Run `/gsd:validate-phase {N}` for each to complete.

---

## Milestone Definition of Done

| Criterion | Status |
|-----------|--------|
| Secrets scrubbed from docker-compose.yml | ✓ |
| .dockerignore prevents credential leakage | ✓ |
| .env.example covers all required variables | ✓ |
| Production Docker stack with nginx reverse proxy | ✓ |
| Single port 80 entry point | ✓ |
| Docker healthchecks on all services | ✓ |
| GitHub Actions: pytest, vitest+tsc, gitleaks, docker build | ✓ (human run pending) |
| API key auth on write endpoints — full chain wired | ✓ |
| README.md + LICENSE | ✓ |

**All 9 milestone criteria met. No blockers. Tech debt accumulated.**

---

*Audit completed: 2026-03-14*
*Phases in scope: 27-32 (v6.0 Production Ready)*
*Total requirements: 14 | Satisfied: 14 | Unsatisfied: 0 | Orphaned: 0*
