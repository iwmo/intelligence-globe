---
phase: 37-entity-labels
plan: 05
type: execute
wave: 4
depends_on: [37-03]
files_modified:
  - frontend/src/components/MilitaryAircraftLayer.tsx
  - frontend/src/components/ShipLayer.tsx
autonomous: true
gap_closure: true
requirements: [LBL-05, LBL-06, LBL-07, LBL-08]

must_haves:
  truths:
    - "Military aircraft labels appear on initial load when showEntityLabels=true and data has arrived"
    - "Ship labels appear on initial load when showEntityLabels=true and data has arrived"
  artifacts:
    - path: "frontend/src/components/MilitaryAircraftLayer.tsx"
      provides: "Effect 4 dep array includes militaryAircraft.data"
    - path: "frontend/src/components/ShipLayer.tsx"
      provides: "Effect 4 dep array includes ships.data"
  key_links:
    - from: "MilitaryAircraftLayer Effect 4"
      to: "militaryAircraft.data"
      via: "React dependency array"
      pattern: "showEntityLabels, layerVisible, militaryAircraft\\.data"
    - from: "ShipLayer Effect 4"
      to: "ships.data"
      via: "React dependency array"
      pattern: "showEntityLabels, layerVisible, ships\\.data"
---

<objective>
Fix the missing data dependency in the label visibility effects of MilitaryAircraftLayer and ShipLayer. Without the data dep, Effect 4 fires once on mount (when the label maps are empty) and never again when data arrives and labels are created — labels stay show:false permanently on initial load.

Purpose: Same root cause as Gap 3 diagnosed in 37-UAT.md. One-line fix per file, no logic changes.
Output: MilitaryAircraftLayer and ShipLayer with correctly wired label visibility effects.
</objective>

<execution_context>
@/Users/joaoribeiro/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joaoribeiro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-entity-labels/37-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add militaryAircraft.data to MilitaryAircraftLayer Effect 4 dependency array</name>
  <files>frontend/src/components/MilitaryAircraftLayer.tsx</files>
  <action>
One targeted change in MilitaryAircraftLayer.tsx.

Locate Effect 4 — the label visibility toggle effect (around line 169):

```typescript
// Effect 4: Label visibility toggle
useEffect(() => {
  for (const [hex, lbl] of militaryLabelsByHex) {
    const bb = militaryBillboardsByHex.get(hex);
    lbl.show = showEntityLabels && (bb?.show ?? false);
  }
}, [showEntityLabels, layerVisible]);
```

Change the dependency array from `[showEntityLabels, layerVisible]` to `[showEntityLabels, layerVisible, militaryAircraft.data]`:

```typescript
}, [showEntityLabels, layerVisible, militaryAircraft.data]);
```

The variable `militaryAircraft` is already in scope (declared at the top of the component via `const militaryAircraft = useMilitaryAircraft()`). Adding `.data` to deps ensures the effect re-runs after Effect 2 populates `militaryLabelsByHex`, so labels that were created with `show: false` get their visibility corrected before the user sees the globe.

Do not change any other part of the effect body or any other effect.
  </action>
  <verify>
    <automated>cd /Users/joaoribeiro/Desktop/INTELLIGENCE\ GLOBE/frontend && npx tsc --noEmit 2>&1 | grep -E "MilitaryAircraftLayer|error TS" | head -20; echo "EXIT:$?"</automated>
  </verify>
  <done>TypeScript compiles with no errors. Grep `grep -n "militaryAircraft.data" frontend/src/components/MilitaryAircraftLayer.tsx` shows a match on the Effect 4 dependency array line (not only in Effect 2).</done>
</task>

<task type="auto">
  <name>Task 2: Add ships.data to ShipLayer Effect 4 dependency array</name>
  <files>frontend/src/components/ShipLayer.tsx</files>
  <action>
One targeted change in ShipLayer.tsx.

Locate Effect 4 — the label visibility toggle effect (around line 177):

```typescript
// Effect 4: Label visibility toggle
useEffect(() => {
  for (const [mmsi, lbl] of shipLabelsByMmsi) {
    const bb = shipBillboardsByMmsi.get(mmsi);
    lbl.show = showEntityLabels && (bb?.show ?? false);
  }
}, [showEntityLabels, layerVisible]);
```

Change the dependency array from `[showEntityLabels, layerVisible]` to `[showEntityLabels, layerVisible, ships.data]`:

```typescript
}, [showEntityLabels, layerVisible, ships.data]);
```

The variable `ships` is already in scope (declared via `const ships = useShips()` or similar at the top of the component). Adding `.data` to deps ensures the effect re-runs after Effect 2 populates `shipLabelsByMmsi`, so labels created with `show: false` get their visibility corrected on initial data load.

Do not change any other part of the effect body or any other effect.
  </action>
  <verify>
    <automated>cd /Users/joaoribeiro/Desktop/INTELLIGENCE\ GLOBE/frontend && npx tsc --noEmit 2>&1 | grep -E "ShipLayer|error TS" | head -20; echo "EXIT:$?"</automated>
  </verify>
  <done>TypeScript compiles with no errors. Grep `grep -n "ships.data" frontend/src/components/ShipLayer.tsx` shows a match on the Effect 4 dependency array line (in addition to the Effect 2 line).</done>
</task>

</tasks>

<verification>
After both tasks:
1. `cd frontend && npx tsc --noEmit` — zero TypeScript errors
2. `grep -n "\[showEntityLabels, layerVisible\]" frontend/src/components/MilitaryAircraftLayer.tsx` — returns nothing (old dep array gone)
3. `grep -n "\[showEntityLabels, layerVisible\]" frontend/src/components/ShipLayer.tsx` — returns nothing (old dep array gone)
4. `grep -n "militaryAircraft\.data" frontend/src/components/MilitaryAircraftLayer.tsx` — shows ≥2 matches (Effect 2 and Effect 4)
5. `grep -n "ships\.data" frontend/src/components/ShipLayer.tsx` — shows ≥2 matches (Effect 2 and Effect 4)
</verification>

<success_criteria>
- MilitaryAircraftLayer Effect 4 deps: [showEntityLabels, layerVisible, militaryAircraft.data]
- ShipLayer Effect 4 deps: [showEntityLabels, layerVisible, ships.data]
- Zero TypeScript errors introduced
- Both label maps (militaryLabelsByHex, shipLabelsByMmsi) will be re-evaluated for visibility whenever new data arrives, eliminating the stuck-show:false state on initial load
</success_criteria>

<output>
After completion, create `.planning/phases/37-entity-labels/37-entity-labels-05-SUMMARY.md`
</output>
