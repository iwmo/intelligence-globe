---
phase: 27-secrets-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.yml
  - .env.example
  - backend/.dockerignore
  - frontend/.dockerignore
autonomous: true
requirements:
  - SEC-01
  - SEC-02
  - SEC-03
must_haves:
  truths:
    - "Running `docker compose config` with no .env file produces an error naming the credential variable, not a silently-substituted secret value"
    - "A .env file present in backend/ or frontend/ during `docker build` is excluded from the image layer"
    - "Any operator can copy .env.example to .env and have every required variable name present with a descriptive placeholder"
  artifacts:
    - path: "docker-compose.yml"
      provides: "Credential variables use ${VAR:?message} syntax — no :- fallbacks for secrets"
      contains: "OPENSKY_CLIENT_SECRET:?Set"
    - path: "backend/.dockerignore"
      provides: "Excludes .env and build artifacts from backend Docker build context"
      contains: ".env"
    - path: "frontend/.dockerignore"
      provides: "Excludes .env and build artifacts from frontend Docker build context"
      contains: ".env"
    - path: ".env.example"
      provides: "All credential variables with descriptive placeholder values"
      contains: "OPENSKY_CLIENT_SECRET=your-opensky-client-secret"
  key_links:
    - from: "docker-compose.yml"
      to: ".env"
      via: "${VAR:?msg} substitution"
      pattern: "OPENSKY_CLIENT_SECRET:\\?Set"
    - from: "backend/.dockerignore"
      to: "backend/Dockerfile COPY . ."
      via: "Docker build context filtering"
      pattern: "\\.env"
    - from: ".env.example"
      to: "operator .env setup"
      via: "copy-paste onboarding"
      pattern: "API_KEY=your"
---

<objective>
Scrub all hardcoded credential fallbacks from docker-compose.yml, create .dockerignore files for both service build contexts, and expand .env.example to list every required variable with descriptive placeholder values.

Purpose: Any credential previously embedded as a :- default in docker-compose.yml is now in git history. This plan removes the living source of the leak and ensures future runs fail loudly — not silently — when credentials are absent.
Output: 4 files modified/created; running `docker compose config` with no .env file shows a hard error naming each unset credential.
</objective>

<execution_context>
@/Users/joaoribeiro/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joaoribeiro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-secrets-cleanup/27-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strip credential fallbacks from docker-compose.yml</name>
  <files>docker-compose.yml</files>
  <action>
Edit docker-compose.yml to replace all credential :- fallback substitutions with ${VAR:?message} mandatory-error syntax. Apply to every occurrence across all services.

Changes required (apply ALL — variables appear in multiple service blocks):

1. `backend` service environment (lines ~42-43) and `worker` service environment (lines ~60-61):
   - `${OPENSKY_CLIENT_ID:-REDACTED_OPENSKY_CLIENT_ID}` → `${OPENSKY_CLIENT_ID:?Set OPENSKY_CLIENT_ID in .env}`
   - `${OPENSKY_CLIENT_SECRET:-REDACTED_OPENSKY_SECRET}` → `${OPENSKY_CLIENT_SECRET:?Set OPENSKY_CLIENT_SECRET in .env}`

2. `ais-worker` service environment (line ~76):
   - `${AISSTREAM_API_KEY:-f7580cbd850ebc8fb86094df59f9a0f5e96aef4d}` → `${AISSTREAM_API_KEY:?Set AISSTREAM_API_KEY in .env}`

3. `frontend` service environment (line ~85):
   - `${VITE_CESIUM_ION_TOKEN:-}` → `${VITE_CESIUM_ION_TOKEN:?Set VITE_CESIUM_ION_TOKEN in .env}`

4. `POSTGRES_PASSWORD` in all three DATABASE_URL composite strings (backend, worker, ais-worker) and the postgres service block:
   - `${POSTGRES_PASSWORD:-postgres}` → `${POSTGRES_PASSWORD:?Set POSTGRES_PASSWORD in .env}` (every occurrence in DATABASE_URL and the postgres service environment block)

DO NOT change these (safe non-credential infrastructure defaults — keep as-is):
   - `${POSTGRES_DB:-opensignal}` — database name, not a secret
   - `${POSTGRES_USER:-postgres}` — username, not sensitive
   - `${FRONTEND_ORIGIN:-http://localhost:3000}` — safe default
   - `${VERSION:-0.1.0}` — safe default
   - Healthcheck on postgres service uses `${POSTGRES_USER:-postgres}` and `${POSTGRES_DB:-opensignal}` — leave those unchanged

Note: The POSTGRES_PASSWORD healthcheck line does NOT appear in the healthcheck — only POSTGRES_USER and POSTGRES_DB do. Only the environment block and DATABASE_URL references need updating for POSTGRES_PASSWORD.

After editing, run `grep ":-" docker-compose.yml` to confirm no credential fallbacks remain. Any remaining `:-` hits should only be for POSTGRES_DB, POSTGRES_USER, FRONTEND_ORIGIN, or VERSION.
  </action>
  <verify>
    <automated>grep ":-" /Users/joaoribeiro/Desktop/INTELLIGENCE\ GLOBE/docker-compose.yml | grep -v "POSTGRES_DB\|POSTGRES_USER\|FRONTEND_ORIGIN\|VERSION" | grep -v "pg_isready" && echo "FAIL: credential fallbacks remain" || echo "PASS: no credential fallbacks"</automated>
  </verify>
  <done>All credential variables (OPENSKY_CLIENT_ID, OPENSKY_CLIENT_SECRET, AISSTREAM_API_KEY, VITE_CESIUM_ION_TOKEN, POSTGRES_PASSWORD) use ${VAR:?message} syntax. Running docker compose config without .env produces a hard error naming the missing variable.</done>
</task>

<task type="auto">
  <name>Task 2: Create .dockerignore files for backend and frontend</name>
  <files>backend/.dockerignore, frontend/.dockerignore</files>
  <action>
Create two .dockerignore files co-located with each service's Dockerfile. They MUST be in the same directory as the Dockerfile (i.e., backend/.dockerignore and frontend/.dockerignore) — NOT at the project root. Docker uses the build context directory to locate .dockerignore; a root-level file has no effect on these builds.

backend/.dockerignore content:
```
# Secrets — never copy into image
.env
*.env
.env.*
.env.local
.env.*.local

# Build artifacts (reduce context size)
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.coverage
htmlcov/
```

frontend/.dockerignore content:
```
# Secrets — never copy into image
.env
*.env
.env.*
.env.local
.env.*.local

# Build artifacts (reduce context size)
node_modules/
dist/
.vite/
```

Use the Write tool to create both files. Do not use shell heredocs.
  </action>
  <verify>
    <automated>ls /Users/joaoribeiro/Desktop/INTELLIGENCE\ GLOBE/backend/.dockerignore /Users/joaoribeiro/Desktop/INTELLIGENCE\ GLOBE/frontend/.dockerignore && grep "\.env" /Users/joaoribeiro/Desktop/INTELLIGENCE\ GLOBE/backend/.dockerignore && grep "\.env" /Users/joaoribeiro/Desktop/INTELLIGENCE\ GLOBE/frontend/.dockerignore && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>backend/.dockerignore and frontend/.dockerignore exist, both contain .env exclusion patterns, and are co-located with their respective Dockerfiles so Docker build context filtering applies.</done>
</task>

<task type="auto">
  <name>Task 3: Expand .env.example with all required credential variables</name>
  <files>.env.example</files>
  <action>
Replace .env.example with the complete version below. The current file has POSTGRES_DB, POSTGRES_USER, POSTGRES_PASSWORD, FRONTEND_ORIGIN, VERSION, VITE_CESIUM_ION_TOKEN (empty), and VITE_API_BASE_URL — but is missing OPENSKY_CLIENT_ID, OPENSKY_CLIENT_SECRET, AISSTREAM_API_KEY, and API_KEY.

The updated file must:
- Use descriptive placeholder values (not empty strings, not real secrets)
- Include comments explaining where to obtain each credential
- List ALL variables the compose stack references, in logical groupings
- Fix VITE_CESIUM_ION_TOKEN from empty `=` to a descriptive placeholder

Write this exact content to .env.example:

```
# PostgreSQL
POSTGRES_DB=opensignal
POSTGRES_USER=postgres
POSTGRES_PASSWORD=changeme

# Backend
FRONTEND_ORIGIN=http://localhost:3000
VERSION=0.1.0

# API Authentication — used by Phase 28 middleware to protect write endpoints
API_KEY=your-secret-api-key

# OpenSky Network OAuth2 — register at https://opensky-network.org/
OPENSKY_CLIENT_ID=your-opensky-client-id
OPENSKY_CLIENT_SECRET=your-opensky-client-secret

# AISStream.io — get your key at https://aisstream.io/
AISSTREAM_API_KEY=your-aisstream-api-key

# Cesium Ion — get your free token at https://ion.cesium.com/tokens
VITE_CESIUM_ION_TOKEN=your-cesium-ion-token

# Frontend
VITE_API_BASE_URL=http://localhost:8000
```

Use the Write tool to write this file.
  </action>
  <verify>
    <automated>grep -E "OPENSKY_CLIENT_ID|OPENSKY_CLIENT_SECRET|AISSTREAM_API_KEY|VITE_CESIUM_ION_TOKEN|API_KEY" /Users/joaoribeiro/Desktop/INTELLIGENCE\ GLOBE/.env.example | wc -l | xargs -I{} sh -c 'if [ {} -ge 5 ]; then echo "PASS: all 5 credential vars present"; else echo "FAIL: missing vars, found {} of 5"; fi'</automated>
  </verify>
  <done>.env.example contains all 5 required credential variables (OPENSKY_CLIENT_ID, OPENSKY_CLIENT_SECRET, AISSTREAM_API_KEY, VITE_CESIUM_ION_TOKEN, API_KEY) with descriptive placeholder values and source comments. No empty placeholder values remain.</done>
</task>

</tasks>

<verification>
After all tasks complete, run the full SEC-01 smoke test by temporarily hiding .env:

```bash
# From project root — assumes .env exists with real values
mv .env /tmp/.env.test 2>/dev/null; docker compose config 2>&1 | grep -iE "(missing|required|Set)"; mv /tmp/.env.test .env 2>/dev/null
```

Expected: Output shows error messages like "required variable OPENSKY_CLIENT_SECRET is missing a value: Set OPENSKY_CLIENT_SECRET in .env" — the credential variable NAME is shown, not its value.

SEC-02 check:
```bash
ls backend/.dockerignore frontend/.dockerignore && grep ".env" backend/.dockerignore frontend/.dockerignore
```

SEC-03 check:
```bash
grep -E "OPENSKY_CLIENT_ID|OPENSKY_CLIENT_SECRET|AISSTREAM_API_KEY|VITE_CESIUM_ION_TOKEN|API_KEY" .env.example
```
</verification>

<success_criteria>
1. `grep ":-" docker-compose.yml` returns only non-credential defaults (POSTGRES_DB, POSTGRES_USER, FRONTEND_ORIGIN, VERSION, healthcheck line)
2. `ls backend/.dockerignore frontend/.dockerignore` returns both paths without error
3. `grep -c "your-" .env.example` returns 5 or more (all credential placeholders use descriptive "your-" values)
4. `docker compose config` with no .env file produces a hard error identifying each unset credential variable by name
</success_criteria>

<output>
After completion, create `.planning/phases/27-secrets-cleanup/27-01-SUMMARY.md` using the summary template.
</output>
