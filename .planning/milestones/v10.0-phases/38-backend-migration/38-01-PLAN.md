---
phase: 38-backend-migration
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - backend/tests/test_ingest_adsbiol.py
  - backend/tests/test_ingest_aircraft.py
autonomous: true
requirements: [INGEST-01, INGEST-02, INGEST-03, INGEST-04, INGEST-05, SCHEMA-01, SCHEMA-02, SCHEMA-03, SCHEMA-04, SCHEMA-05, SCHEMA-06]

must_haves:
  truths:
    - "All INGEST-* and SCHEMA-* requirements have a corresponding failing test that documents the expected behavior"
    - "test_ingest_aircraft.py no longer references OPENSKY_CLIENT_ID, fetch_opensky_token, or app.workers.ingest_aircraft (stale module paths)"
    - "Running pytest tests/test_ingest_adsbiol.py produces ImportError or AttributeError (RED) because ingest_adsbiol.py does not yet exist"
  artifacts:
    - path: "backend/tests/test_ingest_adsbiol.py"
      provides: "Unit tests for all INGEST-* and SCHEMA-* requirements"
      exports: ["test_null_position_filtered", "test_parse_commercial_aircraft", "test_military_url_has_filter_mil", "test_base_url_configurable", "test_no_opensky_references", "test_bbox_param_format", "test_no_bbox_when_redis_empty", "test_ground_altitude_normalised", "test_emergency_field_stored", "test_nav_modes_field", "test_speed_fields", "test_roll_field", "test_registration_type_fields"]
    - path: "backend/tests/test_ingest_aircraft.py"
      provides: "Retired OpenSky-specific tests; no reference to deleted functions or env vars"
  key_links:
    - from: "backend/tests/test_ingest_adsbiol.py"
      to: "backend/app/tasks/ingest_adsbiol.py"
      via: "import app.tasks.ingest_adsbiol"
      pattern: "from app\\.tasks\\.ingest_adsbiol import"
---

<objective>
Write the test scaffold for the ADSB.lol ingest worker before any implementation exists.

Purpose: Establish RED tests for all 11 requirements so Plan 03's implementation can be driven by these tests (TDD). Tests import from `app.tasks.ingest_adsbiol` which does not exist yet — all tests must fail at import time until Plan 03 creates that module.

Output: `tests/test_ingest_adsbiol.py` with 13 named test functions covering every INGEST-* and SCHEMA-* requirement. `tests/test_ingest_aircraft.py` cleaned of stale OpenSky-specific imports and env var references.
</objective>

<execution_context>
@/Users/joaoribeiro/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joaoribeiro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-backend-migration/38-RESEARCH.md
@.planning/phases/38-backend-migration/38-VALIDATION.md
</context>

<interfaces>
<!-- The module under test does not exist yet. These are the contracts the tests assert. -->
<!-- Executor: write tests that import from app.tasks.ingest_adsbiol — all will fail RED. -->

Expected exports from backend/app/tasks/ingest_adsbiol.py (Plan 03 will create):
```python
def parse_adsbiol_aircraft(ac: dict) -> dict | None: ...
async def ingest_commercial_aircraft() -> int: ...
async def ingest_military_aircraft() -> int: ...
def sync_ingest_commercial() -> None: ...
def sync_ingest_military() -> None: ...
```

Redis key used (verified in existing codebase):
  Key: "globe:viewport_bbox"
  Format: "min_lat,min_lon,max_lat,max_lon"
  Mapping to ADSB.lol ?box=: box={min_lat},{max_lat},{min_lon},{max_lon}

ADSB.lol live response shape (verified):
```json
{
  "ac": [{"hex": "a1b2c3", "lat": 37.6, "lon": -122.4, "alt_baro": 35000,
           "gs": 480, "track": 270.0, "baro_rate": -64, "r": "N12345",
           "t": "B738", "emergency": "none", "nav_modes": ["autopilot"],
           "ias": 280, "tas": 460, "mach": 0.78, "roll": 0.0, "flight": "UAL123 "}],
  "total": 4717
}
```

Existing test patterns to replicate (from backend/tests/test_ingest_military.py):
- Use AsyncMock + MagicMock for session mocking
- Use patch("app.tasks.ingest_adsbiol.httpx.AsyncClient") for HTTP mocking
- Use patch("app.tasks.ingest_adsbiol.AsyncSessionLocal") for DB mocking
</interfaces>

<tasks>

<feature>
  <name>ADSB.lol ingest test scaffold</name>
  <files>backend/tests/test_ingest_adsbiol.py, backend/tests/test_ingest_aircraft.py</files>
  <behavior>
    Task 1 — Create test_ingest_adsbiol.py (RED tests, import will fail until Plan 03):

    test_null_position_filtered (INGEST-01):
      - input: ac dict with lat=None — parse_adsbiol_aircraft returns None
      - input: ac dict with lon=None — parse_adsbiol_aircraft returns None

    test_parse_commercial_aircraft (INGEST-01):
      - input: full ac dict with all fields
      - expected: returned dict has icao24=ac["hex"], callsign stripped, latitude, longitude, baro_altitude (numeric), velocity=ac["gs"], true_track=ac["track"], vertical_rate=ac["baro_rate"], registration=ac["r"], type_code=ac["t"], emergency, nav_modes, ias, tas, mach, roll

    test_military_url_has_filter_mil (INGEST-02):
      - Mock httpx to capture the URL called by ingest_military_aircraft()
      - Assert URL contains "filter_mil" query param
      - Assert URL does NOT contain "filter_mil" when ingest_commercial_aircraft() is called

    test_base_url_configurable (INGEST-03):
      - Set env ADSBIO_BASE_URL to "http://custom-host:9999"
      - Mock httpx to capture called URL
      - Run ingest_commercial_aircraft() with mocked HTTP + DB
      - Assert URL starts with "http://custom-host:9999"

    test_no_opensky_references (INGEST-04):
      - Read the contents of backend/app/tasks/ingest_adsbiol.py at test time
      - Assert "OPENSKY_CLIENT_ID" not in file contents
      - Assert "fetch_opensky_token" not in file contents
      - Assert "opensky-network.org" not in file contents
      Note: this test will fail until Plan 03 creates the file; skip with pytest.importorskip or catch FileNotFoundError and xfail

    test_bbox_param_format (INGEST-05):
      - Mock Redis to return b"10.0,20.0,50.0,60.0" for key "globe:viewport_bbox"
      - Mock httpx to capture URL
      - Run ingest_commercial_aircraft() with mocked HTTP + DB
      - Assert URL contains "box=10.0,50.0,20.0,60.0" (lat_s=min_lat=10.0, lat_n=max_lat=50.0, lon_w=min_lon=20.0, lon_e=max_lon=60.0)

    test_no_bbox_when_redis_empty (INGEST-05):
      - Mock Redis to return None for key "globe:viewport_bbox"
      - Mock httpx to capture URL
      - Run ingest_commercial_aircraft() with mocked HTTP + DB
      - Assert URL does NOT contain "box="

    test_ground_altitude_normalised (SCHEMA-01):
      - input: ac dict with alt_baro="ground" — parse_adsbiol_aircraft returns dict with baro_altitude=None
      - input: ac dict with alt_baro=35000 — parse_adsbiol_aircraft returns dict with baro_altitude=35000 (no conversion)
      - input: ac dict with alt_baro=1000 — parse_adsbiol_aircraft returns dict with baro_altitude=1000

    test_emergency_field_stored (SCHEMA-02):
      - input: ac dict with emergency="none" — returned dict has emergency="none" (string, not None)
      - input: ac dict with emergency="general" — returned dict has emergency="general"
      - input: ac dict missing "emergency" key — returned dict has emergency=None

    test_nav_modes_field (SCHEMA-03):
      - input: ac dict with nav_modes=["autopilot", "althold"] — returned dict has nav_modes=["autopilot", "althold"]
      - input: ac dict with nav_modes absent — returned dict has nav_modes=None (not [])

    test_speed_fields (SCHEMA-04):
      - input: ac dict with ias=280, tas=460, mach=0.78 — returned dict has those exact values
      - input: ac dict with ias/tas/mach absent — returned dict has ias=None, tas=None, mach=None

    test_roll_field (SCHEMA-05):
      - input: ac dict with roll=15.5 — returned dict has roll=15.5
      - input: ac dict with roll absent — returned dict has roll=None

    test_registration_type_fields (SCHEMA-06):
      - input: ac dict with r="N12345", t="B738" — returned dict has registration="N12345", type_code="B738"
      - input: ac dict with r and t absent — returned dict has registration=None, type_code=None

    Task 2 — Update test_ingest_aircraft.py:
      - Remove the test functions that import from "app.workers.ingest_aircraft" (stale path — that module never existed under that path)
      - Remove or update the test functions that patch OPENSKY_CLIENT_ID, fetch_opensky_token, or fetch_aircraft_states from the old module
      - Keep test_trail_capped_at_20 if build_new_trail still exists (it does not in the ingest_aircraft task — it was inline) — remove it
      - Keep any test that tests a function that still exists in app.tasks.ingest_aircraft, or mark the entire file as skipped with a comment "OpenSky ingest retired in Phase 38 — see test_ingest_adsbiol.py"
      - Simplest approach: replace the file body with a single module-level comment and a pytest.skip marker so the file does not cause collection errors

  </behavior>
  <implementation>
    Write test_ingest_adsbiol.py with all 13 test functions. Each test imports from app.tasks.ingest_adsbiol.
    For tests that mock HTTP + DB + Redis, use patch() at the correct module path (app.tasks.ingest_adsbiol.*).
    For test_no_opensky_references: read the source file path using pathlib and assert string absence; handle FileNotFoundError by marking the test xfail with reason="ingest_adsbiol.py not yet created".
    For tests that call ingest_commercial_aircraft() or ingest_military_aircraft() with mocked HTTP: return {"ac": [minimal_valid_ac_dict], "total": 1} from the mock response.
    For the DB session mock: use AsyncMock() for session.execute and session.commit; return an AsyncMock with __aiter__ returning empty list for the SELECT result.
    Update test_ingest_aircraft.py: replace file content with module docstring explaining retirement + pytest.skip at module level.
  </implementation>
</feature>

</tasks>

<verification>
RED phase — these tests must fail at collection or import time because app.tasks.ingest_adsbiol does not exist yet:

```bash
cd /Users/joaoribeiro/Desktop/INTELLIGENCE\ GLOBE/backend && python -m pytest tests/test_ingest_adsbiol.py -x -q 2>&1 | head -20
```

Expected output: ImportError or ModuleNotFoundError referencing app.tasks.ingest_adsbiol.

test_ingest_aircraft.py must no longer fail due to stale imports:

```bash
cd /Users/joaoribeiro/Desktop/INTELLIGENCE\ GLOBE/backend && python -m pytest tests/test_ingest_aircraft.py -v 2>&1 | head -10
```

Expected output: skipped (not errors).
</verification>

<success_criteria>
- tests/test_ingest_adsbiol.py exists with all 13 named test functions
- pytest tests/test_ingest_adsbiol.py fails with ImportError (RED — implementation not yet written)
- pytest tests/test_ingest_aircraft.py reports all tests as skipped or collected=0 (no errors)
- No test in test_ingest_adsbiol.py references OPENSKY_CLIENT_ID, fetch_opensky_token, or any OpenSky URL
</success_criteria>

<output>
After completion, create `.planning/phases/38-backend-migration/38-01-SUMMARY.md`
</output>
