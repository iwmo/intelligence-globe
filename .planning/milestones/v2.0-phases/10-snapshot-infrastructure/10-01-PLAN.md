---
phase: 10-snapshot-infrastructure
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - backend/tests/test_snapshot.py
  - backend/tests/test_replay.py
autonomous: true
requirements:
  - REP-01

must_haves:
  truths:
    - "test_snapshot_from_aircraft, test_snapshot_from_military, test_snapshot_from_ship exist and FAIL with ImportError (deferred import pattern)"
    - "test_ensure_partition_name exists and FAILS with ImportError"
    - "test_replay_route_exists, test_replay_layer_filter, test_replay_invalid_layer exist and FAIL with ImportError or 404"
  artifacts:
    - path: "backend/tests/test_snapshot.py"
      provides: "RED-phase unit stubs for snapshot pure helpers"
      exports: ["test_snapshot_from_aircraft", "test_snapshot_from_military", "test_snapshot_from_ship", "test_ensure_partition_name"]
    - path: "backend/tests/test_replay.py"
      provides: "RED-phase integration stubs for replay API route"
      exports: ["test_replay_route_exists", "test_replay_layer_filter", "test_replay_invalid_layer"]
  key_links:
    - from: "backend/tests/test_snapshot.py"
      to: "backend/app/tasks/snapshot_positions.py"
      via: "deferred import inside test body"
      pattern: "from app.tasks.snapshot_positions import"
    - from: "backend/tests/test_replay.py"
      to: "GET /api/replay/snapshots"
      via: "AsyncClient ASGITransport"
      pattern: "client.get.*api/replay/snapshots"
---

<objective>
Create the Wave 0 RED-phase test stubs that define the contracts for snapshot helper functions and the replay API endpoint before any implementation exists.

Purpose: Establish the test-driven contract that Plans 02 and 03 must satisfy. Tests must fail at collection or assertion time — failing tests are the goal here.
Output: Two test files with 7 failing tests total.
</objective>

<execution_context>
@/Users/joaoribeiro/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joaoribeiro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-snapshot-infrastructure/10-RESEARCH.md
@.planning/phases/10-snapshot-infrastructure/10-VALIDATION.md

<interfaces>
<!-- Existing test pattern from test_military.py — use same AsyncClient/ASGITransport approach -->
<!-- Deferred import pattern (established in Phase 09): import inside test body, not at top level -->

From backend/tests/test_military.py:
```python
import pytest
from httpx import AsyncClient, ASGITransport
from app.main import app

@pytest.mark.asyncio
async def test_list_military():
    async with AsyncClient(transport=ASGITransport(app=app), base_url="http://test") as client:
        response = await client.get("/api/military/")
    assert response.status_code == 200
```

Deferred import pattern (used in Phase 09 GPS jamming tests):
```python
def test_snapshot_from_aircraft():
    from app.tasks.snapshot_positions import snapshot_from_aircraft  # deferred — ModuleNotFoundError is the RED signal
    ...
```

Aircraft model fields (for snapshot shape assertion):
  icao24, latitude, longitude, baro_altitude (altitude), velocity (speed), true_track (heading)

MilitaryAircraft model fields:
  hex, latitude, longitude, alt_baro (altitude), gs (speed), track (heading)

Ship model fields:
  mmsi, latitude, longitude, sog (speed), true_heading (heading)
  (no altitude field — altitude=None for ships)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing unit stubs for snapshot pure helpers</name>
  <files>backend/tests/test_snapshot.py</files>
  <behavior>
    - test_snapshot_from_aircraft: import snapshot_from_aircraft from app.tasks.snapshot_positions (deferred, inside body); call with a mock aircraft row dict {icao24, latitude, longitude, baro_altitude, velocity, true_track}; assert returned dict has keys: ts, layer_type="aircraft", entity_id, latitude, longitude, altitude, heading, speed
    - test_snapshot_from_military: import snapshot_from_military (deferred); call with {hex, latitude, longitude, alt_baro, gs, track}; assert returned dict has keys: ts, layer_type="military", entity_id, latitude, longitude, altitude, heading, speed
    - test_snapshot_from_ship: import snapshot_from_ship (deferred); call with {mmsi, latitude, longitude, sog, true_heading}; assert returned dict has keys: ts, layer_type="ship", entity_id, latitude, longitude, altitude=None, heading, speed
    - test_ensure_partition_name: import ensure_partition_name from app.tasks.snapshot_positions (deferred); call with date(2026, 3, 12); assert returns string "position_snapshots_2026_03_12"
  </behavior>
  <action>
    Create backend/tests/test_snapshot.py with 4 tests.

    Use deferred imports (import inside each test function body, NOT at module top level). This ensures the file is collectible even when app.tasks.snapshot_positions does not yet exist — ModuleNotFoundError raised at test execution is the correct RED signal.

    For test_snapshot_from_aircraft, test_snapshot_from_military, test_snapshot_from_ship: pass a datetime as ts value (use datetime(2026, 3, 12, 10, 0, 0, tzinfo=timezone.utc) as a fixed ts arg — the helpers accept ts as a parameter so tests control it).

    For test_ensure_partition_name: this is a pure string function (no DB), takes a date and returns a string. Assert "position_snapshots_2026_03_12".

    Do NOT use pytest.mark.asyncio on these tests — snapshot_from_* and ensure_partition_name are synchronous pure functions.

    Run: cd backend && python -m pytest tests/test_snapshot.py -x -q
    Expected: 4 ERRORS (ModuleNotFoundError) — this is the correct RED state.
  </action>
  <verify>
    <automated>cd /Users/joaoribeiro/Desktop/INTELLIGENCE\ GLOBE/backend && python -m pytest tests/test_snapshot.py -x -q 2>&1 | tail -10</automated>
  </verify>
  <done>4 tests collected, all fail with ModuleNotFoundError on app.tasks.snapshot_positions — RED phase confirmed</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write failing integration stubs for replay API route</name>
  <files>backend/tests/test_replay.py</files>
  <behavior>
    - test_replay_route_exists: GET /api/replay/snapshots?layer=all&start=2026-03-12T00:00:00Z&end=2026-03-12T01:00:00Z must return 200 (not 404/422) with body containing "snapshots" key
    - test_replay_layer_filter: GET /api/replay/snapshots?layer=aircraft&start=2026-03-12T00:00:00Z&end=2026-03-12T01:00:00Z must return 200 with "snapshots" key and "count" key
    - test_replay_invalid_layer: assert response.status_code == 404 for layer=unknown_xyz (route must exist and validate the layer param — returns 404 not 422)
  </behavior>
  <action>
    Create backend/tests/test_replay.py with 3 async tests.

    Use the established AsyncClient(ASGITransport(app=app), base_url="http://test") pattern from test_military.py. Use top-level import (from app.main import app) — this is an integration test, app must import for it to work. The RED signal is 404 (route not yet mounted) or assertion error on response body shape.

    For test_replay_invalid_layer: the route will validate layer against {"aircraft", "military", "ship", "all"} and return 404 for unknown values — assert status_code == 404.

    Run: cd backend && python -m pytest tests/test_replay.py -x -q
    Expected: 3 FAILED (404 or AssertionError since route is not yet mounted) — correct RED state.
  </action>
  <verify>
    <automated>cd /Users/joaoribeiro/Desktop/INTELLIGENCE\ GLOBE/backend && python -m pytest tests/test_replay.py -x -q 2>&1 | tail -10</automated>
  </verify>
  <done>3 tests collected, all fail (404 on route not mounted or AssertionError) — RED phase confirmed</done>
</task>

</tasks>

<verification>
All 7 tests collected and failing (RED):
- 4 unit tests in test_snapshot.py: ModuleNotFoundError
- 3 integration tests in test_replay.py: 404 or AssertionError

Run: cd backend && python -m pytest tests/test_snapshot.py tests/test_replay.py -x -q
</verification>

<success_criteria>
Both test files exist. All 7 tests collected and failing for the correct reason (missing implementation). No syntax errors in test files. Full suite still passes (existing tests unaffected): cd backend && python -m pytest -x -q --ignore=tests/test_snapshot.py --ignore=tests/test_replay.py
</success_criteria>

<output>
After completion, create .planning/phases/10-snapshot-infrastructure/10-01-SUMMARY.md
</output>
