---
phase: 07-visual-engine-navigation
plan: 03
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified:
  - frontend/src/components/CinematicHUD.tsx
  - frontend/src/components/__tests__/MGRSReadout.test.ts
autonomous: true
requirements:
  - VIS-03
  - VIS-04

must_haves:
  truths:
    - "User sees a persistent cinematic HUD with classification banner, MGRS coordinates that update as camera moves, satellite telemetry readout, and REC timestamp"
    - "User can toggle Clean UI mode to hide all sidebars while the HUD remains visible"
    - "HUD does not block camera pan, zoom, or entity click-pick interactions"
  artifacts:
    - path: "frontend/src/components/CinematicHUD.tsx"
      provides: "Cinematic tactical overlay — classification banner, MGRS, telemetry, REC timestamp, Clean UI toggle"
      exports: ["CinematicHUD"]
  key_links:
    - from: "frontend/src/components/CinematicHUD.tsx"
      to: "viewer.camera.moveEnd"
      via: "addEventListener in useEffect to update MGRS on camera move"
      pattern: "moveEnd"
    - from: "frontend/src/components/CinematicHUD.tsx"
      to: "frontend/src/store/useAppStore.ts"
      via: "reads selectedSatelliteId for telemetry, reads/writes cleanUI"
      pattern: "cleanUI|selectedSatelliteId"
    - from: "frontend/src/App.tsx"
      to: "frontend/src/components/CinematicHUD.tsx"
      via: "rendered unconditionally (not wrapped in !cleanUI guard)"
      pattern: "CinematicHUD"
---

<objective>
Build CinematicHUD.tsx — the persistent tactical overlay — implementing the classification banner, live MGRS coordinates, satellite telemetry readout, REC timestamp, and the Clean UI toggle button. Also fills the MGRSReadout test stub with real assertions.

Purpose: VIS-03 and VIS-04 are the "feels operational" requirements — the visual identity that makes this look like an intelligence tool rather than a satellite tracker. The HUD must not interfere with globe interactions (pointer-events pattern is critical).
Output: CinematicHUD.tsx component ready for App.tsx wiring in Plan 05.
</objective>

<execution_context>
@/Users/joaoribeiro/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joaoribeiro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-visual-engine-navigation/07-RESEARCH.md
@.planning/phases/07-visual-engine-navigation/07-01-SUMMARY.md

@frontend/src/store/useAppStore.ts
@frontend/src/App.tsx
@frontend/src/components/LeftSidebar.tsx

<interfaces>
<!-- From frontend/src/store/useAppStore.ts (extended in Plan 01) -->
```typescript
export type VisualPreset = 'normal' | 'nvg' | 'crt' | 'flir' | 'noir';

// Store slices available:
const { cleanUI, setCleanUI, selectedSatelliteId } = useAppStore();
```

<!-- From mgrs@2.1.0 (installed in Plan 02) -->
```typescript
import { forward } from 'mgrs';
// forward([lon, lat], precision) => string
// precision 4 = 10m resolution
// Throws for lat > 84 or lat < -80 (polar/UPS zone) — wrap in try/catch
```

<!-- From cesium (CesiumJS camera API) -->
```typescript
viewer.camera.moveEnd.addEventListener(handler: () => void): void;
viewer.camera.moveEnd.removeEventListener(handler: () => void): void;
viewer.camera.positionCartographic.longitude: number; // radians
viewer.camera.positionCartographic.latitude: number;  // radians
viewer.camera.positionCartographic.height: number;    // meters above ellipsoid
// CesiumMath.toDegrees(radians) => number
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build CinematicHUD component with MGRS readout, telemetry, and Clean UI toggle</name>
  <files>frontend/src/components/CinematicHUD.tsx, frontend/src/components/__tests__/MGRSReadout.test.ts</files>
  <behavior>
    - Test: `getCameraGridRef([51.53, 25.28])` returns a string matching MGRS pattern `^[0-9]{1,2}[A-Z]{3}[0-9]+$` (Doha is in grid 38R)
    - Test: `getCameraGridRef([0, 85])` returns `'UPS'` (lat > 84 degrees)
    - Test: `getCameraGridRef([0, -81])` returns `'UPS'` (lat < -80 degrees)
    - Test: `getCameraGridRef([0, 0])` returns a non-empty string (equatorial zone)
  </behavior>
  <action>
    Extract a pure `getCameraGridRef(lonLat: [number, number]): string` helper function from `CinematicHUD.tsx` and export it. This function:
    - Takes `[lon, lat]` in degrees
    - Returns `'UPS'` for lat > 84 or lat < -80
    - Otherwise calls `forward([lon, lat], 4)` from `mgrs` package and returns the result
    - Wraps `forward()` in try/catch — returns `'---'` on error (e.g., extreme coordinates near polar edge zones)

    **CinematicHUD.tsx structure:**
    Props: `viewer: Viewer | null`

    Root element: `<div style={{ position: 'fixed', inset: 0, zIndex: 80, pointerEvents: 'none', fontFamily: 'monospace' }}>` — this is the full-viewport overlay. `pointer-events: none` is MANDATORY on this root. Any interactive children (buttons) must set `style={{ pointerEvents: 'auto' }}` explicitly.

    **Corner elements (all `position: absolute`):**

    1. **Classification Banner** — top center, full width, `top: 0`:
       - Background: semi-transparent dark green (`rgba(0, 80, 0, 0.7)`)
       - Text: `TOP SECRET // SI // TK // NOFORN` in monospace, uppercase, letter-spaced
       - Static display, no interactivity

    2. **MGRS Readout** — top-right corner, `top: 36px, right: 12px`:
       - Shows: current MGRS grid ref (updates on `camera.moveEnd`)
       - Below: altitude in km (e.g., `ALT: 4523 km`)
       - Below: lat/lon in decimal degrees (4 decimal places)
       - State: `const [mgrsStr, setMgrsStr] = useState('...')`
       - Effect: `useEffect(() => { if (!viewer) return; const handler = () => { const cart = viewer.camera.positionCartographic; const lon = CesiumMath.toDegrees(cart.longitude); const lat = CesiumMath.toDegrees(cart.latitude); setMgrsStr(getCameraGridRef([lon, lat])); setAltKm(Math.round(cart.height / 1000)); setLatLon([lat.toFixed(4), lon.toFixed(4)]); }; viewer.camera.moveEnd.addEventListener(handler); return () => viewer.camera.moveEnd.removeEventListener(handler); }, [viewer]);`

    3. **Telemetry Panel** — bottom-right corner, `bottom: 40px, right: 12px`:
       - Shows satellite telemetry for `selectedSatelliteId` from store
       - If no satellite selected: show placeholder lines with `--` values
       - Labels: `ORB: --`, `PASS: --`, `GSD: --`, `SUN EL: --`
       - Keep static for Phase 7 — real satellite data integration is a future enhancement. Display current selectedSatelliteId or "NO SAT SELECTED" if null. The research notes this is acceptable per open question #2.

    4. **REC Timestamp** — top-left, `top: 36px, left: 12px`:
       - Shows: `REC ●` with a slowly pulsing red dot (CSS animation `@keyframes pulse { 0%, 100% { opacity: 1; } 50% { opacity: 0.3; } }` with 2s duration)
       - Shows live time in UTC: `2026-03-11T14:32:05Z` — update with `setInterval` every second

    5. **Clean UI Toggle button** — bottom-left corner, `bottom: 40px, left: 12px`:
       - `pointerEvents: 'auto'` (explicitly re-enabled on this element only)
       - Shows: `[CLEAN UI]` or `[FULL UI]` based on `cleanUI` state
       - Clicking calls `setCleanUI(!cleanUI)`
       - Consistent dark/monospace styling with rest of HUD

    **MGRSReadout.test.ts** — replace stubs with real tests for `getCameraGridRef`:
    Import the actual function: `import { getCameraGridRef } from '../CinematicHUD'`
    Write the four behavior assertions above as real `it()` tests (not `it.todo`).
    No mocking needed — `getCameraGridRef` is pure (no CesiumJS dependency).
    Note: `mgrs` package must be installed (done in Plan 02 Task 1).
  </action>
  <verify>
    <automated>cd /Users/joaoribeiro/Desktop/INTELLIGENCE\ GLOBE/frontend && npx vitest run src/components/__tests__/MGRSReadout.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>All four getCameraGridRef tests pass. CinematicHUD.tsx compiles without TypeScript errors. The exported getCameraGridRef function handles polar edge cases. Full vitest suite exits 0.</done>
</task>

</tasks>

<verification>
TypeScript: `cd /Users/joaoribeiro/Desktop/INTELLIGENCE GLOBE/frontend && npx tsc --noEmit 2>&1 | head -20`
Tests: `cd /Users/joaoribeiro/Desktop/INTELLIGENCE GLOBE/frontend && npx vitest run 2>&1 | tail -10`
</verification>

<success_criteria>
- CinematicHUD.tsx renders five overlay elements: classification banner, MGRS readout, telemetry panel, REC timestamp, Clean UI toggle
- Root container has `pointer-events: none`; only Clean UI button has `pointer-events: auto`
- getCameraGridRef correctly handles polar regions (returns 'UPS') and normal lat/lon ranges
- MGRSReadout tests pass (4/4)
- Full vitest suite green
</success_criteria>

<output>
After completion, create `.planning/phases/07-visual-engine-navigation/07-03-SUMMARY.md`
</output>
