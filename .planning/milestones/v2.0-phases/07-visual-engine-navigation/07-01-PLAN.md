---
phase: 07-visual-engine-navigation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/store/useAppStore.ts
  - frontend/src/store/__tests__/useAppStore.test.ts
  - frontend/src/components/__tests__/PostProcessEngine.test.tsx
  - frontend/src/components/__tests__/MGRSReadout.test.ts
  - frontend/src/data/__tests__/landmarks.test.ts
  - frontend/src/hooks/__tests__/useKeyboardShortcuts.test.ts
autonomous: true
requirements:
  - VIS-01
  - VIS-02
  - VIS-03
  - VIS-04
  - NAV-02
  - NAV-03

must_haves:
  truths:
    - "Zustand store exports visualPreset, setVisualPreset, postProcessUniforms, setPostProcessUniforms, cleanUI, setCleanUI"
    - "All Wave 0 test stub files exist and run without TypeScript errors"
    - "Existing store tests continue to pass after extension"
  artifacts:
    - path: "frontend/src/store/useAppStore.ts"
      provides: "Extended Zustand store with visual + navigation slices"
      contains: "visualPreset"
    - path: "frontend/src/store/__tests__/useAppStore.test.ts"
      provides: "Tests for new cleanUI and visualPreset slices"
    - path: "frontend/src/components/__tests__/PostProcessEngine.test.tsx"
      provides: "Test stubs for VIS-01 (stage count stability, preset toggle)"
    - path: "frontend/src/components/__tests__/MGRSReadout.test.ts"
      provides: "Test stubs for VIS-03 MGRS conversion"
    - path: "frontend/src/data/__tests__/landmarks.test.ts"
      provides: "Test stubs for NAV-02 landmarks.json schema"
    - path: "frontend/src/hooks/__tests__/useKeyboardShortcuts.test.ts"
      provides: "Test stubs for NAV-03 Q/W/E/R/T keyboard mapping"
  key_links:
    - from: "frontend/src/store/useAppStore.ts"
      to: "frontend/src/components/PostProcessEngine.tsx"
      via: "useAppStore hook consuming visualPreset + postProcessUniforms"
      pattern: "visualPreset"
    - from: "frontend/src/store/useAppStore.ts"
      to: "frontend/src/App.tsx"
      via: "cleanUI flag controlling conditional rendering"
      pattern: "cleanUI"
---

<objective>
Extend the Zustand store with Phase 7 slices and create all Wave 0 test stub files that downstream plans depend on.

Purpose: All Wave 2 plans (PostProcessEngine, CinematicHUD, Navigation) consume store slices from this plan. Wave 0 test stubs must exist before implementation tasks can use them as verify targets. Building the store first enforces the interface-first ordering pattern.
Output: Extended useAppStore.ts, extended useAppStore.test.ts, four new test stub files covering VIS-01/02/03/04 and NAV-02/03.
</objective>

<execution_context>
@/Users/joaoribeiro/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joaoribeiro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-visual-engine-navigation/07-RESEARCH.md

@frontend/src/store/useAppStore.ts
@frontend/src/store/__tests__/useAppStore.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend useAppStore with visual engine and clean UI slices</name>
  <files>frontend/src/store/useAppStore.ts, frontend/src/store/__tests__/useAppStore.test.ts</files>
  <behavior>
    - Test: `useAppStore.getState().visualPreset` defaults to `'normal'`
    - Test: `setVisualPreset('nvg')` sets `visualPreset` to `'nvg'`
    - Test: `useAppStore.getState().postProcessUniforms.bloomIntensity` defaults to `0.5`
    - Test: `setPostProcessUniforms({ bloomIntensity: 1.2 })` merges (does not replace) — `gainAmount` remains unchanged
    - Test: `useAppStore.getState().cleanUI` defaults to `false`
    - Test: `setCleanUI(true)` sets `cleanUI` to `true`; `setCleanUI(false)` sets it back
    - Test: All existing tests (satelliteFilter, aircraftFilter, aircraftLastUpdated, sidebarOpen, layers) continue to pass
  </behavior>
  <action>
    Add three new slices to `useAppStore.ts`:

    1. `visualPreset: VisualPreset` — type union `'normal' | 'nvg' | 'crt' | 'flir' | 'noir'`, default `'normal'`
    2. `postProcessUniforms: PostProcessUniforms` — object containing `bloomIntensity: number` (0.5), `sharpenAmount: number` (0.5), `gainAmount: number` (1.0), `scanlineSpacing: number` (3), `pixelationLevel: number` (1)
    3. `cleanUI: boolean` — default `false`

    Export the `VisualPreset` and `PostProcessUniforms` types from `useAppStore.ts` so downstream components can import them without re-declaring.

    Add setter: `setVisualPreset: (preset: VisualPreset) => void` — simple `set({ visualPreset: preset })`
    Add setter: `setPostProcessUniforms: (u: Partial<PostProcessUniforms>) => void` — merges into existing uniforms
    Add setter: `setCleanUI: (v: boolean) => void` — simple `set({ cleanUI: v })`

    In `useAppStore.test.ts`: add a new `describe` block `'visual engine and clean UI slices'` covering all behavior items above. Add the new slices to the `beforeEach` reset block. Do NOT modify existing describe blocks.
  </action>
  <verify>
    <automated>cd /Users/joaoribeiro/Desktop/INTELLIGENCE\ GLOBE/frontend && npx vitest run src/store/__tests__/useAppStore.test.ts --reporter=verbose 2>&1 | tail -25</automated>
  </verify>
  <done>All tests in useAppStore.test.ts pass, including new visual engine and clean UI tests. TypeScript compiles without errors on store file.</done>
</task>

<task type="auto">
  <name>Task 2: Create Wave 0 test stub files for PostProcessEngine, MGRSReadout, landmarks, and keyboard shortcuts</name>
  <files>
    frontend/src/components/__tests__/PostProcessEngine.test.tsx,
    frontend/src/components/__tests__/MGRSReadout.test.ts,
    frontend/src/data/__tests__/landmarks.test.ts,
    frontend/src/hooks/__tests__/useKeyboardShortcuts.test.ts
  </files>
  <action>
    Create four test stub files. Each file imports the not-yet-created module it will test, uses `describe` + `it.todo` for not-yet-implemented assertions, and one concrete failing-safe test to verify the stub itself runs.

    **PostProcessEngine.test.tsx** — covers VIS-01/VIS-02:
    - Import `{ PostProcessEngine }` from `'../PostProcessEngine'` (will fail at runtime until Plan 02)
    - Describe: `'PostProcessEngine — stage lifecycle'`
    - `it.todo('enables only the nvg stage when preset is nvg')`
    - `it.todo('disables all stages when preset is normal')`
    - `it.todo('postProcessStages length is stable after 5 preset switches')`
    - `it.todo('uniform function returns updated value after slider change')`
    - One concrete test: `it('module is importable', () => { expect(true).toBe(true) })` — this passes even before the component exists because the import is dynamic

    IMPORTANT: The import of PostProcessEngine must use `vi.mock` or a try/catch wrapper so the file does not error on missing module. Use:
    ```tsx
    vi.mock('../PostProcessEngine', () => ({ PostProcessEngine: () => null }));
    ```
    This makes the file runnable now (returns green stubs) and will be overwritten with real assertions in Plan 02.

    **MGRSReadout.test.ts** — covers VIS-03:
    - Import `{ getCameraGridRef }` from `'../MGRSReadout'` wrapped with `vi.mock('../MGRSReadout', () => ({ getCameraGridRef: () => '38RMQ' }))`
    - Describe: `'getCameraGridRef — MGRS conversion'`
    - `it.todo('returns valid MGRS string for Doha coordinates [51.53, 25.28]')`
    - `it.todo('returns UPS for polar lat > 84')`
    - `it.todo('returns UPS for polar lat < -80')`
    - Concrete test: `it('module is importable', () => { expect(true).toBe(true) })`

    **landmarks.test.ts** — covers NAV-02:
    - Import landmarks data file: `import landmarks from '../../data/landmarks.json'` (will be created in Plan 04)
    - Wrap with `vi.mock('../../data/landmarks.json', () => ({ default: { landmarks: [] } }))`
    - Describe: `'landmarks.json — schema validation'`
    - `it.todo('contains at least 5 landmarks')`
    - `it.todo('each landmark has id, name, shortcut, lat, lon, altMeters fields')`
    - `it.todo('Q/W/E/R/T shortcuts each appear exactly once')`
    - Concrete test: `it('module is importable', () => { expect(true).toBe(true) })`

    **useKeyboardShortcuts.test.ts** — covers NAV-03:
    - Import: `vi.mock('../../hooks/useKeyboardShortcuts', () => ({ useKeyboardShortcuts: () => undefined }))`
    - Describe: `'useKeyboardShortcuts — keyboard shortcut dispatch'`
    - `it.todo('keydown Q triggers flyToLandmark with first landmark id')`
    - `it.todo('keydown W triggers flyToLandmark with second landmark id')`
    - `it.todo('keydown T triggers flyToLandmark with fifth landmark id')`
    - `it.todo('no flyTo called when no viewer is registered')`
    - Concrete test: `it('module is importable', () => { expect(true).toBe(true) })`
  </action>
  <verify>
    <automated>cd /Users/joaoribeiro/Desktop/INTELLIGENCE\ GLOBE/frontend && npx vitest run src/components/__tests__/PostProcessEngine.test.tsx src/components/__tests__/MGRSReadout.test.ts src/data/__tests__/landmarks.test.ts src/hooks/__tests__/useKeyboardShortcuts.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>All four stub files run under vitest with zero failures (only `todo` items and the concrete `module is importable` passing). Full suite still green: `npx vitest run` exits 0.</done>
</task>

</tasks>

<verification>
Run full suite to confirm Wave 1 is clean:
`cd /Users/joaoribeiro/Desktop/INTELLIGENCE GLOBE/frontend && npx vitest run 2>&1 | tail -10`

Expected: all existing tests pass, new store tests pass, stub files pass.
</verification>

<success_criteria>
- `useAppStore` exports `VisualPreset`, `PostProcessUniforms` types and three new slices with setters
- `visualPreset` defaults to `'normal'`, `cleanUI` defaults to `false`, `postProcessUniforms` defaults to the five documented values
- All seven Wave 0 test stubs exist and are vitest-runnable (zero failures, only todos)
- Full vitest suite exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/07-visual-engine-navigation/07-01-SUMMARY.md`
</output>
