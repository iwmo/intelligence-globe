---
phase: 26-end-to-end-verification-stale-indicators
plan: 03
type: execute
wave: 2
depends_on: [26-01]
files_modified: []
autonomous: false
requirements: [VRFY-01]

must_haves:
  truths:
    - "A 2-hour replay scrub with all 6 layers active completes without contamination"
    - "Playback auto-stops at replayWindowEnd and play button returns to initial state"
    - "GPS jamming badge visible throughout playback"
    - "Street traffic particles absent during playback"
    - "Return to LIVE delivers fresh entity positions within 5 seconds"
  artifacts: []
  key_links:
    - from: "PlaybackBar tick() auto-stop"
      to: "useAppStore.setIsPlaying(false)"
      via: "rAF loop terminates when next >= replayWindowEnd"
      pattern: "setIsPlaying\\(false\\)"
---

<objective>
VRFY-01 manual end-to-end verification — confirm the full 2-hour replay scrub is clean across all 6 layers and auto-stop works correctly at the window boundary.

Purpose: Automated contract tests verify the tick() logic in isolation. This checkpoint verifies the assembled system works with real Cesium rendering and real snapshot data.

Output: User confirmation that all Phase 25 + 26 behavioral contracts hold in the running application.
</objective>

<execution_context>
@/Users/joaoribeiro/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joaoribeiro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/26-end-to-end-verification-stale-indicators/26-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start the application for manual verification</name>
  <files></files>
  <action>
Start the full application stack so the user can perform the manual scrub test.

Run in the background:
```bash
cd "/Users/joaoribeiro/Desktop/INTELLIGENCE GLOBE" && docker compose up -d
```

Wait for services to be healthy:
```bash
cd "/Users/joaoribeiro/Desktop/INTELLIGENCE GLOBE" && docker compose ps
```

Then start the frontend dev server:
```bash
cd "/Users/joaoribeiro/Desktop/INTELLIGENCE GLOBE/frontend" && npm run dev &
```

Application should be accessible at http://localhost:5173 (or whatever port Vite reports).

Confirm that `docker compose ps` shows backend and database containers as healthy before signalling ready for the checkpoint.
  </action>
  <verify>
    <automated>cd "/Users/joaoribeiro/Desktop/INTELLIGENCE GLOBE" && docker compose ps 2>&1 | grep -E "running|healthy|Up"</automated>
  </verify>
  <done>Docker services running. Frontend dev server started. Application accessible at localhost dev URL.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: VRFY-01 end-to-end manual scrub verification</name>
  <action>User performs 5-step manual verification of the full replay system.</action>
  <verify>User approval via resume-signal.</verify>
  <done>User confirms all 5 checks pass.</done>
  <what-built>
Full application stack running with all Phase 23–26 changes applied:
- All 6 layers active (aircraft, ships, military, GPS jamming, street traffic, satellites)
- Stale-tint effects live in aircraft, ships, military layers (26-02)
- PlaybackBar auto-stop at replayWindowEnd
- All Phase 25 layer guards in place
  </what-built>
  <how-to-verify>
Open the application at http://localhost:5173 (or reported Vite URL) and perform the following checks:

**Check 1 — LIVE mode stale tinting (VIS-01):**
- Ensure you are in LIVE mode (not playback)
- Look at aircraft, ships, and military aircraft on the globe
- Any entity whose backend data is stale should appear grey/translucent compared to fresh entities
- Fresh entities appear at normal colors (orange aircraft, green ships, red military)

**Check 2 — Enter PLAYBACK mode:**
- Click PLAYBACK button
- Verify CinematicHUD changes from REC to REPLAY + timestamp
- Verify entities appear at FULL color in playback (no grey tint — stale-tint effect early-returns in playback)

**Check 3 — Scrub through the 2-hour window:**
- Move the scrubber from start to end (or let playback run at 15m/s)
- Watch for any layer showing entities at wrong positions (contamination)
- Satellites should animate at replay timestamp positions
- GPS jamming badge should be visible (amber LIVE DATA badge)
- Street traffic particles should NOT appear (hidden in playback)

**Check 4 — Auto-stop at window end:**
- Let playback run at 15m/s until it reaches the window end OR scrub to rightmost and press PLAY
- Verify: play button returns to PLAY state (auto-stopped), scrubber is at the right end

**Check 5 — Return to LIVE:**
- Click LIVE button
- Wait up to 5 seconds
- Verify entity positions update to current live data within that time
- Street traffic particles should reappear
  </how-to-verify>
  <resume-signal>Type "approved" if all 5 checks pass, or describe any failures found</resume-signal>
</task>

</tasks>

<verification>
Manual verification by user (see checkpoint above).
</verification>

<success_criteria>
- All 5 manual checks confirmed by user
- No layer contamination observed during scrub
- Auto-stop works at window end
- Stale tinting visible in LIVE mode (or absence noted if all entities currently fresh)
- Return to LIVE delivers fresh data within 5 seconds
</success_criteria>

<output>
After completion, create `.planning/phases/26-end-to-end-verification-stale-indicators/26-03-SUMMARY.md`
</output>
