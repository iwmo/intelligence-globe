---
phase: 34-backend-foundation
plan: 02
type: tdd
wave: 2
depends_on:
  - "34-01"
files_modified:
  - backend/app/tasks/ingest_gdelt.py
autonomous: true
requirements:
  - GDELT-02
  - GDELT-04

must_haves:
  truths:
    - "RQ worker fetches `lastupdate.txt`, extracts the `.export.CSV.zip` URL, downloads and parses it in-memory with no disk I/O"
    - "Rows with null/empty lat or lon are skipped before any DB insert"
    - "Rows with quad_class == 1 (Verbal Cooperation) are skipped at parse time"
    - "SQLDATE (YYYYMMDD) parsed as midnight UTC `occurred_at`; DATEADDED (YYYYMMDDHHmmss) parsed as full UTC `discovered_at`"
    - "Two back-to-back ingest cycles for the same file produce zero duplicate rows (ON CONFLICT DO NOTHING + Redis file-level skip)"
    - "7-day rolling cleanup deletes rows where occurred_at < now - 7 days before the upsert loop runs"
    - "Worker self-re-enqueues every 900 seconds (15 minutes) via `enqueue_in` in the `finally` block"
  artifacts:
    - path: "backend/app/tasks/ingest_gdelt.py"
      provides: "parse_export_url, parse_gdelt_row, cleanup_old_events, ingest_gdelt, sync_ingest_gdelt"
      exports: ["sync_ingest_gdelt", "parse_export_url", "parse_gdelt_row", "cleanup_old_events"]
  key_links:
    - from: "backend/app/tasks/ingest_gdelt.py"
      to: "backend/app/models/gdelt_event.py"
      via: "from app.models.gdelt_event import GdeltEvent"
      pattern: "from app\\.models\\.gdelt_event import GdeltEvent"
    - from: "backend/app/tasks/ingest_gdelt.py"
      to: "redis:gdelt:processed_files SET"
      via: "redis_conn.sadd('gdelt:processed_files', url)"
      pattern: "sadd.*processed_files"
    - from: "backend/app/tasks/ingest_gdelt.py"
      to: "PostgreSQL gdelt_events"
      via: "pg_insert(GdeltEvent).on_conflict_do_nothing(index_elements=['global_event_id'])"
      pattern: "on_conflict_do_nothing"
---

<objective>
Implement the GDELT RQ ingest worker: fetches `lastupdate.txt`, downloads the export ZIP in-memory, parses tab-delimited rows, applies three deduplication layers (Redis file-level, null-coord filter, ON CONFLICT DO NOTHING), and runs 7-day rolling cleanup before each upsert batch. Worker self-re-enqueues every 15 minutes.

Purpose: This is the core data pipeline — without it no GDELT events reach PostgreSQL and the API returns empty results.
Output: `backend/app/tasks/ingest_gdelt.py` with all exported functions making their xfail test stubs green.
</objective>

<execution_context>
@/Users/joaoribeiro/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joaoribeiro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34-backend-foundation/34-01-SUMMARY.md
@.planning/phases/34-backend-foundation/34-RESEARCH.md

<interfaces>
<!-- Extracted from codebase — executor needs no exploration. -->

From backend/app/tasks/ingest_aircraft.py (self-re-enqueue + asyncio.run pattern):
```python
POLL_INTERVAL_SECONDS = 90

def sync_ingest_aircraft() -> None:
    try:
        asyncio.run(ingest_aircraft())
    except Exception as exc:
        logger.exception("Aircraft ingest failed: %s", exc)
        raise
    finally:
        from redis import Redis
        from rq import Queue
        redis_url = os.getenv("REDIS_URL", "redis://redis:6379/0")
        conn = Redis.from_url(redis_url)
        q = Queue(connection=conn)
        q.enqueue_in(timedelta(seconds=POLL_INTERVAL_SECONDS), sync_ingest_aircraft)
        logger.info("Re-enqueued aircraft ingest; next run in %ds", POLL_INTERVAL_SECONDS)
```

From backend/app/db.py (async session usage):
```python
from app.db import AsyncSessionLocal
# Usage:
async with AsyncSessionLocal() as session:
    await session.execute(stmt)
    await session.commit()
```

From RESEARCH.md (pg_insert pattern):
```python
from sqlalchemy.dialects.postgresql import insert as pg_insert
stmt = pg_insert(GdeltEvent).values(**row_dict).on_conflict_do_nothing(index_elements=["global_event_id"])
await session.execute(stmt)
```

GdeltEvent model (created in Plan 01):
```python
# backend/app/models/gdelt_event.py
class GdeltEvent(Base):
    __tablename__ = "gdelt_events"
    id, global_event_id, occurred_at, discovered_at, latitude, longitude,
    quad_class, goldstein_scale, event_code, actor1_name, actor2_name,
    source_url, avg_tone, num_mentions, source_is_stale
```

GDELT CSV column positions (0-based, tab-delimited, NO header row):
- 0: GLOBALEVENTID → global_event_id
- 1: SQLDATE (YYYYMMDD) → occurred_at (midnight UTC)
- 6: Actor1Name → actor1_name
- 21: Actor2Name → actor2_name
- 27: EventCode (VARCHAR(4)) → event_code
- 30: QuadClass (int 1-4) → quad_class
- 31: GoldsteinScale (float) → goldstein_scale
- 32: NumMentions (int) → num_mentions
- 34: AvgTone (float) → avg_tone
- 47: ActionGeo_Lat (float or empty) → latitude
- 48: ActionGeo_Long (float or empty) → longitude
- 57: DATEADDED (YYYYMMDDHHmmss) → discovered_at (full UTC datetime)
- 58: SOURCEURL → source_url
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement ingest_gdelt.py — pure helpers (parse functions)</name>
  <files>backend/app/tasks/ingest_gdelt.py</files>
  <behavior>
    - test_parse_export_url: `parse_export_url("65336 abc http://example.com/20260314000000.export.CSV.zip\n...")` returns `"http://example.com/20260314000000.export.CSV.zip"`; returns None if no .export.CSV.zip line found
    - test_null_coordinates_skipped: `parse_gdelt_row(row_with_empty_lat)` returns None; `parse_gdelt_row(row_with_empty_lon)` returns None
    - test_quad_class_filter: `parse_gdelt_row(row_with_quad_class_1)` returns None; `parse_gdelt_row(row_with_quad_class_2)` returns a dict
    - test_sqldate_parsing: result dict has `occurred_at == datetime(2026, 3, 14, tzinfo=timezone.utc)` for SQLDATE '20260314'
    - test_dateadded_parsing: result dict has `discovered_at == datetime(2026, 3, 14, 12, 30, 45, tzinfo=timezone.utc)` for DATEADDED '20260314123045'
    - test_event_code_varchar4 (from Plan 01 stubs): `parse_gdelt_row` returns `event_code == '040'` not `40` for row with EventCode field '040'
  </behavior>
  <action>
    Create `backend/app/tasks/ingest_gdelt.py` with the pure helper functions first (RED → GREEN with unit tests):

    **`parse_export_url(text: str) -> str | None`**: Iterate lines; split each by whitespace; return `parts[2]` if `parts[2].endswith(".export.CSV.zip")`. Return None if not found.

    **`parse_gdelt_row(row: list[str]) -> dict | None`**: Follow the exact logic from RESEARCH.md Pattern 3:
    - Return None if `len(row) < 59`
    - Parse lat/lon from row[47]/row[48] — `float(x) if x else None`; return None if either is None
    - Parse quad_class from row[30]; return None if not in `{2, 3, 4}` (hardcoded — no env var needed per research open question resolution)
    - Parse `occurred_at` from row[1] with `datetime.strptime(row[1], "%Y%m%d").replace(tzinfo=timezone.utc)` — return None on ValueError
    - Parse `discovered_at` from row[57] with `datetime.strptime(row[57], "%Y%m%d%H%M%S").replace(tzinfo=timezone.utc) if row[57] else None`
    - Return dict with all fields; `event_code=row[27][:4] if row[27] else None` (truncate defensively to VARCHAR(4)); `source_is_stale=False`

    Place async functions (`download_and_parse_export`, `cleanup_old_events`, `ingest_gdelt`) and the sync wrapper (`sync_ingest_gdelt`) in the same file after the pure helpers. They can be stubs at this stage (raise NotImplementedError) — the unit tests only test the pure functions.

    POLL_INTERVAL_SECONDS = 900. Imports: `import asyncio, io, zipfile, csv, logging, os` from stdlib; `import httpx`; `from datetime import datetime, timedelta, timezone`; `from sqlalchemy.dialects.postgresql import insert as pg_insert`; `from sqlalchemy import delete`; `from app.db import AsyncSessionLocal`; `from app.models.gdelt_event import GdeltEvent`.
  </action>
  <verify>
    <automated>cd /Users/joaoribeiro/Desktop/INTELLIGENCE\ GLOBE/backend && python -m pytest tests/test_gdelt.py::test_parse_export_url tests/test_gdelt.py::test_null_coordinates_skipped tests/test_gdelt.py::test_quad_class_filter tests/test_gdelt.py::test_sqldate_parsing tests/test_gdelt.py::test_dateadded_parsing -x -q</automated>
  </verify>
  <done>5 unit tests pass; parse helpers are implemented and correct per the GDELT 2.0 column spec; no integration tests yet</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement async ingest pipeline and sync wrapper</name>
  <files>backend/app/tasks/ingest_gdelt.py</files>
  <behavior>
    - test_redis_file_level_skip: when `redis_conn.sadd("gdelt:processed_files", url)` returns 0, `ingest_gdelt` (or its caller) returns early without any DB session being opened; mock Redis.from_url to return mock with sadd returning 0
    - test_no_duplicate_rows: insert a row with global_event_id='TEST123', then insert same id again using the pg_insert ON CONFLICT DO NOTHING path — `SELECT COUNT(*) FROM gdelt_events WHERE global_event_id='TEST123'` returns 1
    - test_7day_cleanup: insert a row with occurred_at = now - 8 days; insert a row with occurred_at = now - 1 day; call `cleanup_old_events(session)`; only the 1-day-old row remains
  </behavior>
  <action>
    Implement the remaining async functions in `backend/app/tasks/ingest_gdelt.py`:

    **`cleanup_old_events(session: AsyncSession) -> int`**: Delete rows where `GdeltEvent.occurred_at < datetime.now(timezone.utc) - timedelta(days=7)`. Return `result.rowcount`. Use `await session.execute(delete(GdeltEvent).where(...))`.

    **`download_and_parse_export(url: str) -> list[list[str]]`**: async with httpx.AsyncClient(timeout=60.0); GET the URL (plain HTTP — not HTTPS, per RESEARCH.md Pitfall 7); `zipfile.ZipFile(io.BytesIO(resp.content))`; `zf.namelist()[0]`; decode UTF-8 with errors="replace"; `csv.reader(io.StringIO(text), delimiter="\t")`; return `list(reader)`.

    **`ingest_gdelt() -> None`** (async):
    1. Fetch `http://data.gdeltproject.org/gdeltv2/lastupdate.txt` with httpx (plain HTTP)
    2. Call `parse_export_url(resp.text)` — if None, log warning and return
    3. Check Redis: `redis_conn = Redis.from_url(os.getenv("REDIS_URL", "redis://redis:6379/0"))`; if `redis_conn.sadd("gdelt:processed_files", export_url) == 0`, log "already processed, skipping" and return
    4. Call `download_and_parse_export(export_url)`
    5. Open AsyncSessionLocal: run `cleanup_old_events(session)`, commit
    6. Open second AsyncSessionLocal: for each row in parsed_rows, call `parse_gdelt_row(row)`; skip None results; bulk-insert via `session.execute(pg_insert(GdeltEvent).values(**row_dict).on_conflict_do_nothing(index_elements=["global_event_id"]))`; commit once after the loop
    7. Log count of rows inserted

    **`sync_ingest_gdelt() -> None`**: exact self-re-enqueue pattern from ingest_aircraft.py — `asyncio.run(ingest_gdelt())` inside try/except, re-enqueue in finally with `q.enqueue_in(timedelta(seconds=900), sync_ingest_gdelt)`.

    IMPORTANT: The `redis_conn` used for the SADD check inside `ingest_gdelt` must be a synchronous `redis.Redis` connection (same as the finally block — RQ workers run synchronously). Call `asyncio.get_event_loop()` is NOT needed; use synchronous Redis in the sync wrapper context or pass connection as argument.

    Actually: `ingest_gdelt()` is async but Redis check is synchronous — use `from redis import Redis as SyncRedis` and instantiate `SyncRedis.from_url(...)` inside the async function for the SADD call (this is the existing project pattern — see ingest_gps_jamming.py). This is safe because SADD is a blocking call of <1ms.
  </action>
  <verify>
    <automated>cd /Users/joaoribeiro/Desktop/INTELLIGENCE\ GLOBE/backend && python -m pytest tests/test_gdelt.py::test_redis_file_level_skip tests/test_gdelt.py::test_no_duplicate_rows tests/test_gdelt.py::test_7day_cleanup -x -q</automated>
  </verify>
  <done>3 integration/mock tests pass; `sync_ingest_gdelt` is importable; full unit suite still green: `pytest tests/test_gdelt.py -k "parse or cleanup or redis or duplicate" -q` passes</done>
</task>

</tasks>

<verification>
After both tasks, run the full GDELT test suite:
```bash
cd /Users/joaoribeiro/Desktop/INTELLIGENCE\ GLOBE/backend && python -m pytest tests/test_gdelt.py -q
```
Expected: 8 tests pass (parse_export_url, null_coordinates, quad_class_filter, sqldate_parsing, dateadded_parsing, redis_file_level_skip, no_duplicate_rows, 7day_cleanup). API route tests still xfail (route not registered yet).
</verification>

<success_criteria>
- `backend/app/tasks/ingest_gdelt.py` exports: `parse_export_url`, `parse_gdelt_row`, `cleanup_old_events`, `ingest_gdelt`, `sync_ingest_gdelt`
- 8 GDELT-02 and GDELT-04 tests pass in test_gdelt.py
- `parse_gdelt_row` correctly skips null coords, QuadClass 1, and stores event_code as VARCHAR(4) string
- `cleanup_old_events` deletes rows older than 7 days, preserves recent rows
- `sync_ingest_gdelt` follows self-re-enqueue pattern with `enqueue_in(timedelta(seconds=900), ...)`
</success_criteria>

<output>
After completion, create `.planning/phases/34-backend-foundation/34-02-SUMMARY.md`
</output>
