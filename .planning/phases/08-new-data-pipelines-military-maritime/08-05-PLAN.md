---
phase: 08-new-data-pipelines-military-maritime
plan: "05"
type: execute
wave: 4
depends_on: ["08-04"]
files_modified:
  - frontend/src/App.tsx
  - frontend/src/components/LeftSidebar.tsx
autonomous: false
requirements: [LAY-01, LAY-03]
user_setup:
  - service: aisstream.io
    why: "AIS maritime data WebSocket streaming"
    env_vars:
      - name: AISSTREAM_API_KEY
        source: "aisstream.io — sign up at https://aisstream.io, navigate to API Keys, create key"
    dashboard_config:
      - task: "Create account and generate API key"
        location: "https://aisstream.io"

must_haves:
  truths:
    - "User can enable Military Flights toggle and see amber dots on the globe"
    - "User can click an amber dot and see MilitaryDetailPanel with callsign, type, altitude, speed, heading"
    - "User can enable Maritime Traffic toggle and see cyan ship dots on the globe"
    - "User can click a cyan dot and see ShipDetailPanel with MMSI, speed, heading, last update"
    - "Disabling a layer toggle hides its dots immediately without page reload"
    - "All existing Phase 7 functionality (visual presets, HUD, navigation) still works"
  artifacts:
    - path: "frontend/src/App.tsx"
      provides: "MilitaryAircraftLayer and ShipLayer mounted in component tree"
      contains: "MilitaryAircraftLayer, ShipLayer"
    - path: "frontend/src/components/LeftSidebar.tsx"
      provides: "MIL and SHIP layer toggle buttons alongside existing SAT and AIR buttons"
      contains: "MIL, SHIP"
  key_links:
    - from: "frontend/src/App.tsx"
      to: "frontend/src/components/MilitaryAircraftLayer.tsx"
      via: "<MilitaryAircraftLayer viewer={viewer} /> mounted conditionally or always-on"
      pattern: "MilitaryAircraftLayer"
    - from: "frontend/src/components/LeftSidebar.tsx"
      to: "useAppStore.setLayerVisible"
      via: "LayerToggleButton calling setLayerVisible('militaryAircraft', ...) and setLayerVisible('ships', ...)"
      pattern: "militaryAircraft.*ships"
---

<objective>
Wire MilitaryAircraftLayer and ShipLayer into App.tsx, add MIL and SHIP layer toggle buttons to LeftSidebar, then verify all four Phase 8 requirements in the browser.

Purpose: Connects all backend + frontend work into the running application. Users can see and interact with both new layers.
Output: Complete LAY-01 and LAY-03 implemented and visually verified.
</objective>

<execution_context>
@/Users/joaoribeiro/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joaoribeiro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-visual-engine-navigation/07-05-SUMMARY.md
@frontend/src/components/LeftSidebar.tsx
</context>

<interfaces>
<!-- From 07-05-SUMMARY.md — App.tsx wiring pattern for Phase 8: -->
<!-- Always-on components (no cleanUI gate): PostProcessEngine, CinematicHUD, LandmarkNav -->
<!-- cleanUI-gated components: LeftSidebar, RightDrawer, BottomStatusBar, PostProcessPanel -->
<!-- MilitaryAircraftLayer and ShipLayer should be always-on (they manage their own show/hide via layers toggle) -->

<!-- From LeftSidebar.tsx — LayerToggleButton component (reuse as-is): -->
```tsx
<LayerToggleButton
  label="SAT"
  active={layers.satellites}
  icon={<Satellite size={12} />}
  onToggle={() => setLayerVisible('satellites', !layers.satellites)}
/>
// New buttons to add:
// label="MIL", icon={<CrosshairIcon or ShieldAlert size={12} />}, layer='militaryAircraft'
// label="SHIP", icon={<AnchorIcon or Ship size={12} />}, layer='ships'
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Wire layers into App.tsx and add MIL/SHIP toggles to LeftSidebar</name>
  <files>frontend/src/App.tsx, frontend/src/components/LeftSidebar.tsx</files>
  <action>
    **Step 1 — Update frontend/src/App.tsx:**
    Following the established Phase 7 wiring pattern from 07-05-SUMMARY.md:
    - Import MilitaryAircraftLayer and ShipLayer
    - Mount both components always-on (outside cleanUI gate), alongside other always-on layer components:
      ```tsx
      {viewer && <MilitaryAircraftLayer viewer={viewer} />}
      {viewer && <ShipLayer viewer={viewer} />}
      ```
    - MilitaryAircraftLayer and ShipLayer manage their own visibility via `layers.militaryAircraft` and `layers.ships` in the store — they do not need a conditional mount gate
    - Position them near the existing AircraftLayer and SatelliteLayer mounting points for logical grouping

    **Step 2 — Update frontend/src/components/LeftSidebar.tsx:**
    Add two new LayerToggleButton entries to the persistent layer toggle strip. Check if lucide-react has appropriate icons (ShieldAlert for military, Anchor or Ship for maritime). If the exact icon name is unavailable in the installed lucide-react version, use Crosshair for military and Navigation for ships — both are present in all lucide-react versions. The toggle strip already uses `{ sidebarOpen, setSidebarOpen, layers, setLayerVisible } = useAppStore()` — just add two more LayerToggleButton calls using the existing component:
    ```tsx
    <LayerToggleButton
      label="MIL"
      active={layers.militaryAircraft}
      icon={<ShieldAlert size={12} />}
      onToggle={() => setLayerVisible('militaryAircraft', !layers.militaryAircraft)}
    />
    <LayerToggleButton
      label="SHIP"
      active={layers.ships}
      icon={<Anchor size={12} />}
      onToggle={() => setLayerVisible('ships', !layers.ships)}
    />
    ```
    Place these after the existing SAT and AIR buttons in the toggle strip (bottom-left layer strip).
  </action>
  <verify>
    <automated>cd /Users/joaoribeiro/Desktop/INTELLIGENCE\ GLOBE/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>TypeScript compiles; App.tsx imports and mounts MilitaryAircraftLayer and ShipLayer; LeftSidebar has MIL and SHIP toggle buttons</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Phase 8 implementation:
    - Military aircraft backend: /api/military/ polling airplanes.live /v2/mil every 300s, MilitaryAircraft PostgreSQL table
    - Maritime ships backend: /api/ships/ from Redis/PostgreSQL, ais-worker Docker service connecting to aisstream.io
    - MilitaryAircraftLayer: amber (#F59E0B) dots on globe, 300s refresh, click to inspect
    - ShipLayer: cyan (#06B6D4) dots on globe, 30s refresh, click to inspect
    - MIL and SHIP layer toggles in bottom-left toggle strip
    - MilitaryDetailPanel and ShipDetailPanel in RightDrawer
    - Click handler routes mil:/mmsi: prefixed IDs to correct panels
  </what-built>
  <how-to-verify>
    Ensure docker-compose is running with all services (including the new ais-worker). The AISSTREAM_API_KEY must be set in .env for live ship data.

    **LAY-01 Verification:**
    1. Open the application in browser
    2. Click the "MIL" toggle in the bottom-left strip — verify it activates (blue border)
    3. Wait up to 300 seconds for the first ingest OR manually trigger: `docker compose exec worker python -c "from app.tasks.ingest_military import sync_ingest_military; sync_ingest_military.__wrapped__()"`
    4. Verify amber (#F59E0B) dots appear on the globe in locations where military aircraft are active
    5. Click an amber dot — verify MilitaryDetailPanel opens in the right drawer showing: callsign, ICAO24 hex, aircraft type, altitude (ft), speed (knots), heading
    6. Toggle MIL off — verify amber dots disappear immediately
    7. Toggle MIL back on — verify dots reappear without page reload

    **LAY-03 Verification (requires AISSTREAM_API_KEY):**
    1. Click the "SHIP" toggle — verify it activates
    2. Wait up to 30 seconds for first batch flush from Redis to PostgreSQL
    3. Verify cyan (#06B6D4) dots appear on the globe (especially in shipping lanes: Atlantic, Indian Ocean, South China Sea)
    4. Click a cyan dot — verify ShipDetailPanel opens showing: MMSI, vessel name (may be "Unknown" until ShipStaticData arrives), speed (knots), heading, last update time
    5. Toggle SHIP off — verify cyan dots disappear
    6. If AISSTREAM_API_KEY is not yet set: verify /api/ships/ returns 200 with an empty list (no crash)

    **Regression check:**
    7. Verify visual presets (Normal/NVG/CRT/FLIR/Noir) still work
    8. Verify SAT and AIR toggles still work
    9. Verify clicking a commercial aircraft still opens AircraftDetailPanel (not MilitaryDetailPanel)
    10. Verify clicking a satellite still opens SatelliteDetailPanel
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Full suite: cd /Users/joaoribeiro/Desktop/INTELLIGENCE\ GLOBE/backend && pytest tests/ -x -q && cd ../frontend && npx vitest run
</verification>

<success_criteria>
- LAY-01: User can enable Military Flights toggle; amber icons appear for military aircraft; click-to-inspect shows callsign, ICAO24, type, altitude, speed, heading
- LAY-03: User can enable Maritime Traffic toggle; cyan ship icons appear; click-to-inspect shows MMSI, vessel name, speed, heading, last update time
- Layer toggles hide/show independently without page reload
- No regression in Phase 7 functionality (presets, HUD, navigation)
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/08-new-data-pipelines-military-maritime/08-05-SUMMARY.md`
</output>
