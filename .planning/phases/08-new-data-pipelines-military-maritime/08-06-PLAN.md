---
phase: 08-new-data-pipelines-military-maritime
plan: 06
type: execute
wave: 6
depends_on: [08-05]
files_modified:
  - frontend/src/workers/propagation.worker.ts
autonomous: false
requirements: [LAY-01, LAY-03]
gap_closure: true

must_haves:
  truths:
    - "App starts without repeating TypeError: Cannot read properties of null (reading 'position') in browser console"
    - "All nine UAT tests pass — satellite propagation, military layer, ship layer, regressions"
  artifacts:
    - path: "frontend/src/workers/propagation.worker.ts"
      provides: "Null-safe satellite propagation — guard handles pv === null before accessing pv.position"
      contains: "pv === null ||"
  key_links:
    - from: "satellite.propagate(satrec, now)"
      to: "pv.position access"
      via: "null guard added before existing boolean/undefined guard"
      pattern: "pv === null \\|\\|"
---

<objective>
Fix the repeating browser crash caused by satellite.propagate() returning null for decayed or invalid TLEs.

Purpose: Phase 8 UAT test 1 (Cold Start Smoke Test) failed — the app produces `TypeError: Cannot read properties of null (reading 'position')` continuously. This is a pre-existing bug from Phase 04 that surfaces when TLEs with eccentricity out of range or negative mean motion are loaded. The fix is a targeted two-line null guard addition in propagation.worker.ts.

Output: A stable app start with no repeating TypeError — all 9 Phase 8 UAT tests passing.
</objective>

<execution_context>
@/Users/joaoribeiro/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joaoribeiro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add pv === null guard to propagation.worker.ts</name>
  <files>frontend/src/workers/propagation.worker.ts</files>
  <action>
satellite.propagate() returns null (not just { position: false }) for decayed TLEs — invalid eccentricity, negative mean motion, etc. The current guard `typeof pv.position === 'boolean' || pv.position === undefined` throws a TypeError before it evaluates because pv itself is null.

Fix all three call sites in propagation.worker.ts:

1. PROPAGATE handler (line 64):
   Change:
     if (typeof pv.position === 'boolean' || pv.position === undefined) {
   To:
     if (pv === null || typeof pv.position === 'boolean' || pv.position === undefined) {

2. COMPUTE_ORBIT handler (line 93):
   Change:
     if (typeof pv.position === 'boolean' || pv.position === undefined) continue;
   To:
     if (pv === null || typeof pv.position === 'boolean' || pv.position === undefined) continue;

3. GET_POSITION handler (line 116):
   Change:
     if (typeof pv.position === 'boolean' || pv.position === undefined) {
   To:
     if (pv === null || typeof pv.position === 'boolean' || pv.position === undefined) {

No other changes to the file. Do not alter logic, types, or message handling.

Note: Using `pv === null ||` per root_cause documented in 08-UAT.md. `typeof pv.position === 'boolean'` handles the satellite.js documented return of `{ position: false }` for propagation errors — both null return and boolean-position must be caught.
  </action>
  <verify>
    <automated>cd "/Users/joaoribeiro/Desktop/INTELLIGENCE GLOBE/frontend" && grep -n "pv === null" src/workers/propagation.worker.ts</automated>
  </verify>
  <done>
    Three occurrences of `pv === null ||` appear in propagation.worker.ts — one in each of the PROPAGATE, COMPUTE_ORBIT, and GET_POSITION handlers. Run `npx tsc --noEmit` from the frontend directory to confirm no TypeScript errors.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify clean cold start — no repeating TypeError</name>
  <files>frontend/src/workers/propagation.worker.ts</files>
  <action>Human verification — confirm the null guard fix eliminated the repeating TypeError on cold start.</action>
  <verify>
    <automated>cd "/Users/joaoribeiro/Desktop/INTELLIGENCE GLOBE/frontend" && grep -c "pv === null" src/workers/propagation.worker.ts</automated>
  </verify>
  <done>User confirms no repeating TypeError in browser console on cold start, and all Phase 8 UAT scenarios pass.</done>
  <what-built>Added pv === null null guard at all three satellite.propagate() call sites in propagation.worker.ts. The fix prevents the repeating TypeError that caused UAT Test 1 to fail on cold start.</what-built>
  <how-to-verify>
    1. Kill any running containers: `docker compose down`
    2. Rebuild and start: `docker compose up --build`
    3. Open the app in the browser and open DevTools Console
    4. Confirm: no repeating `TypeError: Cannot read properties of null (reading 'position')` errors
    5. Confirm: no `SES_UNCAUGHT_EXCEPTION: null` entries repeating
    6. Toggle the MIL layer on — amber dots should appear without error
    7. Toggle the SHIP layer on — cyan dots should appear without error
    8. Click a commercial aircraft — detail panel opens (regression check)
    9. The satellite layer continues to animate without crashes
  </how-to-verify>
  <resume-signal>Type "approved" if the console is clean and all layers work. Describe any remaining issues otherwise.</resume-signal>
</task>

</tasks>

<verification>
Run from frontend directory: `grep -c "pv === null" src/workers/propagation.worker.ts` — must return 3.
Run TypeScript check: `cd "/Users/joaoribeiro/Desktop/INTELLIGENCE GLOBE/frontend" && npx tsc --noEmit` — no errors.
</verification>

<success_criteria>
Cold start produces no repeating TypeError in browser console. All 9 Phase 8 UAT tests pass. The satellite propagation worker silently skips null-result TLEs (decayed/invalid orbits) rather than crashing.
</success_criteria>

<output>
After completion, create `.planning/phases/08-new-data-pipelines-military-maritime/08-06-SUMMARY.md`
</output>
