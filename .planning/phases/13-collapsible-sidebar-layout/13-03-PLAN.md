---
phase: 13-collapsible-sidebar-layout
plan: 03
type: execute
wave: 3
depends_on:
  - 13-01
  - 13-02
files_modified: []
autonomous: false
requirements:
  - LAYOUT-01
  - LAYOUT-02
  - LAYOUT-03

must_haves:
  truths:
    - "User can click each section header to collapse it; clicking again expands it with no visible jump"
    - "Collapsing one section does not affect other sections"
    - "Visual preset sliders panel and aircraft filter panel do not overlap at any sidebar scroll position"
    - "Four named sections are visible with clear monospace headers"
    - "Animation does not drop frame rate below 55 FPS during collapse/expand"
  artifacts:
    - path: "frontend/src/components/LeftSidebar.tsx"
      provides: "Four collapsible sections running in browser at 60 FPS"
    - path: "frontend/src/components/CollapsibleSection.tsx"
      provides: "grid-template-rows animation — no scrollHeight, no reflow"
  key_links:
    - from: "CollapsibleSection grid container"
      to: "browser compositor"
      via: "grid-template-rows CSS transition — no JS measurement, no synchronous reflow"
      pattern: "gridTemplateRows.*0fr|1fr"
---

<objective>
Validate that the collapsible sidebar works correctly in a live browser: animation is smooth at 60 FPS, sections are independent, PostProcessPanel overlap is eliminated, and section state survives sidebar close/reopen.

Purpose: Plans 01 and 02 deliver the implementation. This plan confirms it meets the quality bar required before Phase 13 is marked complete. jsdom cannot validate CSS animation smoothness or visual overlap — only a running browser can.
Output: Confirmed pass on all three LAYOUT requirements via human visual inspection.
</objective>

<execution_context>
@/Users/joaoribeiro/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joaoribeiro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/13-collapsible-sidebar-layout/13-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start dev server and run full test suite</name>
  <files></files>
  <action>
    Run the full vitest suite to confirm all tests pass before human verification:

    ```bash
    cd "/Users/joaoribeiro/Desktop/INTELLIGENCE GLOBE/frontend" && npx vitest run
    ```

    Then start the dev server if it is not already running:
    ```bash
    cd "/Users/joaoribeiro/Desktop/INTELLIGENCE GLOBE" && docker-compose up -d
    ```

    Confirm the frontend is accessible at http://localhost:5173 (or the configured dev port). If vite is run directly without docker, the command is:
    ```bash
    cd "/Users/joaoribeiro/Desktop/INTELLIGENCE GLOBE/frontend" && npm run dev
    ```

    Report the URL to the user in the checkpoint below.
  </action>
  <verify>
    <automated>cd "/Users/joaoribeiro/Desktop/INTELLIGENCE GLOBE/frontend" && npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <done>Full vitest suite passes. Dev server running and accessible.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Four collapsible sections in the left sidebar (LAYERS / SEARCH / FILTERS / VISUAL ENGINE), each independently collapsible via a header button with chevron indicator. PostProcessPanel moved from its floating position (which caused the visual engine sliders to overlap the aircraft filter panel) into the VISUAL ENGINE section inside the sidebar scroll container.
  </what-built>
  <how-to-verify>
    Open the app at http://localhost:5173 (or the dev URL reported by Task 1).

    **LAYOUT-01 — Independent collapse/expand with smooth animation:**
    1. Open the sidebar (hamburger button, top-left)
    2. Click the LAYERS section header — it should collapse with a smooth animation (no jump)
    3. Click it again — it should expand smoothly
    4. Collapse FILTERS, then click SEARCH — FILTERS should remain collapsed (independent state)
    5. Close sidebar (click X or hamburger), reopen — FILTERS should still be collapsed (state persists)
    6. Optional: Open Chrome DevTools → Performance tab → Record while clicking a section header — no Layout Shift events, no FPS drop below 55

    **LAYOUT-02 — No overlap between visual preset sliders and aircraft filter panel:**
    1. Make sure FILTERS and VISUAL ENGINE sections are both expanded
    2. Scroll the sidebar to the bottom — the visual preset sliders (VISUAL ENGINE section) should appear below the aircraft filter (FILTERS section), not floating on top of it
    3. Confirm there are NO duplicate VISUAL PRESET buttons (only one set of sliders visible)

    **LAYOUT-03 — Named sections with clear visual hierarchy:**
    1. Confirm four section headers are visible: LAYERS, SEARCH, FILTERS, VISUAL ENGINE
    2. Each header should use monospace font, small caps (10px), cyan color matching the rest of the UI
    3. Each header should have a chevron icon that rotates on collapse/expand

    **Pass criteria:**
    - All three LAYOUT requirements visually confirmed
    - No duplicate PostProcessPanel (no two sets of visual preset sliders)
    - Sections collapse and expand independently
    - Animation has no visible jump or snap
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues for gap closure</resume-signal>
</task>

</tasks>

<verification>
Human visual confirmation at http://localhost:5173 — see checkpoint instructions above.
</verification>

<success_criteria>
- LAYOUT-01: Each of the four sections collapses and expands independently with smooth animation
- LAYOUT-02: PostProcessPanel sliders visible only once, inside sidebar scroll container, no overlap with FilterPanel
- LAYOUT-03: Four named sections (LAYERS, SEARCH, FILTERS, VISUAL ENGINE) with clear monospace headers and chevron indicators
- Full vitest suite green
- Phase 13 ready for `/gsd:verify-work`
</success_criteria>

<output>
After completion, create `.planning/phases/13-collapsible-sidebar-layout/13-03-SUMMARY.md`
</output>
