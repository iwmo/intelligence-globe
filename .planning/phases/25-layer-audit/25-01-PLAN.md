---
phase: 25-layer-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/lib/queryClient.ts
  - frontend/src/main.tsx
  - frontend/src/hooks/__tests__/useGpsJamming.test.ts
  - frontend/src/components/__tests__/AircraftLayer.debounce.test.tsx
  - frontend/src/components/__tests__/ShipLayer.test.tsx
  - frontend/src/components/__tests__/MilitaryAircraftLayer.test.tsx
  - frontend/src/components/__tests__/GpsJammingLayer.test.tsx
  - frontend/src/components/__tests__/StreetTrafficLayer.test.tsx
  - frontend/src/components/__tests__/PlaybackBar.test.tsx
autonomous: true
requirements:
  - PLAY-04
  - LAYR-01
  - LAYR-02
  - LAYR-03
  - LAYR-04

must_haves:
  truths:
    - "queryClient is a shared module-level instance importable by any file without circular deps"
    - "Failing tests exist for every LAYR-01..04 and PLAY-04 behavior before implementations are written"
    - "All new test cases follow RED: they assert behaviors that do not yet exist in source"
  artifacts:
    - path: "frontend/src/lib/queryClient.ts"
      provides: "Shared QueryClient instance"
      exports: ["queryClient"]
    - path: "frontend/src/main.tsx"
      provides: "Updated to import queryClient from lib"
      contains: "from './lib/queryClient'"
    - path: "frontend/src/hooks/__tests__/useGpsJamming.test.ts"
      provides: "Failing test: refetchInterval frozen in playback"
    - path: "frontend/src/components/__tests__/AircraftLayer.debounce.test.tsx"
      provides: "Failing test: lerp skips bb.position in playback"
    - path: "frontend/src/components/__tests__/PlaybackBar.test.tsx"
      provides: "Failing test: invalidateQueries called on return to LIVE"
  key_links:
    - from: "frontend/src/main.tsx"
      to: "frontend/src/lib/queryClient.ts"
      via: "named import"
      pattern: "from './lib/queryClient'"
    - from: "frontend/src/components/__tests__/PlaybackBar.test.tsx"
      to: "frontend/src/lib/queryClient.ts"
      via: "vi.mock + spy on invalidateQueries"
      pattern: "vi.mock.*queryClient"
---

<objective>
Create the shared `queryClient.ts` module and write failing test stubs for all five Phase 25 requirements. This is the Wave 0 scaffold — tests must be RED before implementations turn them GREEN.

Purpose: Establish verifiable contracts for every layer guard before any production code changes. The `queryClient.ts` extraction is done here because it is a prerequisite for PLAY-04 tests and is risk-free (zero behavior change — just moving the instantiation).
Output: One new shared module, one updated entry point, failing test cases added to six existing test files.
</objective>

<execution_context>
@/Users/joaoribeiro/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joaoribeiro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-layer-audit/25-RESEARCH.md

<interfaces>
<!-- Current main.tsx: queryClient created inline, not exported -->
<!-- src/main.tsx line 6: const queryClient = new QueryClient(); -->
<!-- Must become: import { queryClient } from './lib/queryClient'; -->

From frontend/src/hooks/useGpsJamming.ts (current — no replayMode guard):
```typescript
export function useGpsJamming() {
  return useQuery<{ cells: GpsJammingCell[] }>({
    queryKey: ['gps-jamming'],
    staleTime: 86_400_000,
    refetchInterval: 86_400_000,   // NOT gated — test must assert it becomes false in playback
    retry: 3,
    retryDelay: 5_000,
  });
}
```

From frontend/src/components/AircraftLayer.tsx (current lerp — no playback guard):
```typescript
function lerp() {
  if (!rafRunningRef.current) return;
  // Missing: if (useAppStore.getState().replayMode === 'playback') { rAF + return; }
  const alpha = Math.min((Date.now() - lastUpdateTime) / POLL_INTERVAL_MS, 1.0);
  for (const [icao24, bb] of billboardsByIcao24) {
    bb.position = Cartesian3.lerp(prev, curr, alpha, scratchLerp); // writes in playback — WRONG
  }
}
```

From frontend/src/components/PlaybackBar.tsx (handleModeToggle — no invalidation):
```typescript
function handleModeToggle() {
  if (replayMode === 'live') {
    setReplayMode('playback');
  } else {
    useAppStore.getState().setIsPlaying(false);
    setReplayMode('live');
    useAppStore.getState().setReplayTs(Date.now());
    // Missing: queryClient.invalidateQueries()
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract queryClient to shared module</name>
  <files>frontend/src/lib/queryClient.ts, frontend/src/main.tsx</files>
  <action>
    Create `frontend/src/lib/queryClient.ts`:
    ```typescript
    import { QueryClient } from '@tanstack/react-query';
    export const queryClient = new QueryClient();
    ```

    Update `frontend/src/main.tsx` — replace the inline `const queryClient = new QueryClient();` line with `import { queryClient } from './lib/queryClient';`. The `QueryClientProvider` usage and all other lines remain identical. This is a zero-behavior-change refactor: the same QueryClient instance is used, it is now just accessible from a shared module.

    Ensure `frontend/src/lib/` directory exists (create it if absent).
  </action>
  <verify>
    <automated>cd /Users/joaoribeiro/Desktop/INTELLIGENCE\ GLOBE/frontend && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>
    `frontend/src/lib/queryClient.ts` exports `queryClient`. `frontend/src/main.tsx` imports it. Existing test suite stays green (no behavior change).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write failing tests for all Phase 25 behaviors</name>
  <files>
    frontend/src/hooks/__tests__/useGpsJamming.test.ts,
    frontend/src/components/__tests__/AircraftLayer.debounce.test.tsx,
    frontend/src/components/__tests__/ShipLayer.test.tsx,
    frontend/src/components/__tests__/MilitaryAircraftLayer.test.tsx,
    frontend/src/components/__tests__/GpsJammingLayer.test.tsx,
    frontend/src/components/__tests__/StreetTrafficLayer.test.tsx,
    frontend/src/components/__tests__/PlaybackBar.test.tsx
  </files>
  <behavior>
    - LAYR-01: AircraftLayer — when replayMode is 'playback', lerp loop does NOT write bb.position
    - LAYR-02: ShipLayer Effect 2 — when replayMode is 'playback', returns early without calling bb.position =
    - LAYR-02: MilitaryAircraftLayer Effect 2 — same as above
    - LAYR-03: useGpsJamming — when replayMode is 'playback', refetchInterval is false (not 86_400_000)
    - LAYR-03: GpsJammingLayer — when replayMode is 'playback' and layerVisible is true, badge element with text 'GPS LIVE DATA' is rendered
    - LAYR-04: StreetTrafficLayer — when replayMode transitions to 'playback', all particles have show = false
    - PLAY-04: PlaybackBar — when handleModeToggle switches to LIVE, queryClient.invalidateQueries() is called exactly once
  </behavior>
  <action>
    These test cases must be written as NEW `describe` blocks added to the END of each existing test file. Do NOT remove or modify existing test cases.

    **useGpsJamming.test.ts** — create this file (does not exist yet):
    Mock `useAppStore` with `vi.mock('../store/useAppStore', ...)`, inject replayMode as 'playback', call `renderHook(() => useGpsJamming())`, spy on `useQuery` options, assert `refetchInterval === false`. Add a second test with replayMode 'live' asserting `refetchInterval === 86_400_000`.

    **AircraftLayer.debounce.test.tsx** — add `describe('LAYR-01: lerp guard in playback', ...)` block. Mock `useAppStore.getState` to return `replayMode: 'playback'`. Mount the component with a mock viewer. Trigger aircraft data update. Assert `mockBillboard.position` was NOT called (position write was skipped). Use `vi.spyOn` on the billboard's position setter.

    **ShipLayer.test.tsx** — add `describe('LAYR-02: Effect 2 guard', ...)`. Set replayMode to 'playback' before ships.data is set. Provide ships.data. Assert `mockBillboard.position` setter was NOT called during the effect run.

    **MilitaryAircraftLayer.test.tsx** — same pattern as ShipLayer test.

    **GpsJammingLayer.test.tsx** — add `describe('LAYR-03: amber badge', ...)`. Render with `replayMode: 'playback'`, `layers.gpsJamming: true`. Assert `screen.getByText('GPS LIVE DATA')` is present. Add second test with replayMode 'live': assert badge is absent.

    **StreetTrafficLayer.test.tsx** — add `describe('LAYR-04: hide particles in playback', ...)`. Create mock particles in particlesRef. Set replayMode to 'playback'. Assert all `p.primitive.show === false`. Set replayMode back to 'live' with layerVisible true. Assert `p.primitive.show === true`.

    **PlaybackBar.test.tsx** — add `describe('PLAY-04: invalidateQueries on return to LIVE', ...)`. Mock `../lib/queryClient` with `vi.mock`. Spy on `queryClient.invalidateQueries`. Render in playback mode. Click the LIVE toggle button. Assert `invalidateQueries` was called once.

    All tests must fail (RED) because implementations have not been added yet. Run `npx vitest run` and confirm new test cases appear as FAIL before committing.
  </action>
  <verify>
    <automated>cd /Users/joaoribeiro/Desktop/INTELLIGENCE\ GLOBE/frontend && npx vitest run --reporter=verbose 2>&1 | grep -E "(FAIL|PASS|✓|✗|×)" | tail -40</automated>
  </verify>
  <done>
    Seven new `describe` blocks added to six test files (plus one new test file). New test cases FAIL. Existing test cases remain PASS. The vitest output shows the new failing tests by name.
  </done>
</task>

</tasks>

<verification>
`cd frontend && npx vitest run` — existing tests pass, new test stubs fail with descriptive names matching Phase 25 requirement IDs.
</verification>

<success_criteria>
- `frontend/src/lib/queryClient.ts` exists and exports `queryClient`
- `frontend/src/main.tsx` imports from `./lib/queryClient` (not inline)
- `frontend/src/hooks/__tests__/useGpsJamming.test.ts` exists with 2 test cases (both RED)
- 6 existing test files each have a new `describe` block (RED) covering their Phase 25 requirement
- Full suite: existing tests GREEN, new tests RED
</success_criteria>

<output>
After completion, create `.planning/phases/25-layer-audit/25-01-SUMMARY.md`
</output>
