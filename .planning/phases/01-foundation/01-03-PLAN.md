---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified: []
autonomous: false
requirements:
  - GLOB-01
  - GLOB-02
  - INFRA-01
  - INFRA-02

must_haves:
  truths:
    - "docker compose up from a clean checkout brings all four services to healthy state without manual steps"
    - "Browser at localhost:3000 shows a 3D interactive globe with terrain, atmosphere, day/night shading, and star field"
    - "No default CesiumJS chrome is visible — no animation widget, geocoder, home button, base layer picker, or timeline"
    - "Globe background is pure black with neon blue accents on the status bar — no white or gray default styling"
    - "Bottom status bar shows 'OPENSIGNAL GLOBE' branding and 'API connected' state from the FastAPI health endpoint"
    - "pytest backend/tests/test_health.py passes all three tests"
---

<objective>
Human visual verification that the full Phase 1 system is working: Docker stack healthy, globe rendering cinematically, backend communicating, and all automated tests passing.

Purpose: Phase 1's success criteria are partly visual (cinematic theme, globe rendering) and cannot be validated by automated tests alone. This checkpoint is the quality gate before Phase 2 begins.
Output: Confirmed Phase 1 complete — stack healthy, globe cinematic, backend connected, tests green.
</objective>

<execution_context>
@/Users/joaoribeiro/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joaoribeiro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full automated test suite and Docker smoke check</name>
  <files></files>
  <action>
    Run the complete automated verification sequence:

    1. Start Docker Compose stack:
       ```
       docker compose up -d
       ```

    2. Wait for all services to reach healthy state (up to 60 seconds):
       ```
       docker compose ps
       ```
       All four services (postgres, redis, backend, frontend) must show "healthy" or "running".

    3. Run backend test suite:
       ```
       pytest backend/tests/ -v
       ```
       Expected: test_health.py (3 tests) and test_db.py (3 tests) all green.

    4. Verify health endpoint directly:
       ```
       curl -s http://localhost:8000/api/health
       ```
       Expected response: `{"status":"ok","version":"0.1.0"}`

    5. Report results. If any step fails, output the exact error and stop — do not proceed to the human checkpoint.
  </action>
  <verify>
    <automated>cd "/Users/joaoribeiro/Desktop/INTELLIGENCE GLOBE" && docker compose ps && pytest backend/tests/ -v && curl -s http://localhost:8000/api/health</automated>
  </verify>
  <done>All automated checks pass: 6 pytest tests green, health endpoint returns 200, all Docker services healthy.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification — cinematic globe and API connection</name>
  <files></files>
  <action>Present visual verification checklist to user. All automated checks in Task 1 must be green before this checkpoint is reached.</action>
  <verify>
    <automated>N/A — visual rendering cannot be verified programmatically in Phase 1</automated>
  </verify>
  <done>User confirms all six visual items: globe renders, day/night shading active, no CesiumJS chrome, dark cinematic theme, API connected in status bar, globe is interactive.</done>
  <what-built>
    Full Phase 1 stack:
    - Docker Compose: postgres+postgis, redis, FastAPI backend, React frontend
    - CesiumJS globe with cinematic dark theme, terrain, atmosphere, day/night shading
    - App shell: left sidebar (collapsed), bottom status bar with API connection status, right drawer (hidden)
    - Zustand store, TanStack Query health check, Vite build with viteStaticCopy
  </what-built>
  <how-to-verify>
    The stack should already be running from Task 1. Open your browser to:

    http://localhost:3000

    Verify each item:

    1. GLOBE RENDERING — A 3D globe is visible filling the entire browser window. No white background. The globe shows terrain relief, a blue atmospheric glow at the horizon, and a dark star field visible in space.

    2. DAY/NIGHT SHADING — Part of the globe is illuminated (day side) and part is dark (night side). This confirms `enableLighting = true` is working.

    3. NO CESIUM CHROME — None of these are visible: animation widget (bottom left play button), home button, geocoder (search box), base layer picker, timeline bar, navigation help button.

    4. DARK CINEMATIC THEME — Background behind the globe is pure black. The bottom status bar is dark with a thin neon blue border. The branding text "OPENSIGNAL GLOBE" appears in neon blue.

    5. API CONNECTION — The bottom status bar shows "API connected · v0.1.0" (or similar). This confirms the frontend TanStack Query health hook reached the FastAPI backend successfully.

    6. INTERACTIVITY — You can click and drag to rotate the globe. Scroll to zoom in/out. The globe moves smoothly.

    If the Cesium ion token was not set in .env (VITE_CESIUM_ION_TOKEN is empty): the globe may show a simpler ellipsoid (no terrain detail) — this is expected graceful degradation, not a failure.
  </how-to-verify>
  <resume-signal>Type "approved" if all six items check out, or describe any issues you see.</resume-signal>
</task>

</tasks>

<verification>
Automated gate: All 6 pytest tests green + health endpoint 200 + Docker services healthy.
Visual gate: Human confirms globe renders cinematically with no default CesiumJS chrome.
</verification>

<success_criteria>
- 6 automated tests pass (test_health.py: 3, test_db.py: 3)
- `docker compose ps` shows all services healthy
- Human approves visual check: globe cinematic, no chrome, dark theme, API connected status visible
- Phase 1 roadmap success criteria all met (see ROADMAP.md Phase 1)
</success_criteria>

<output>
After approval, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
