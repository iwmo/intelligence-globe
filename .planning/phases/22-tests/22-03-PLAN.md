---
phase: 22-tests
plan: 03
type: execute
wave: 2
depends_on:
  - "22-01"
  - "22-02"
files_modified: []
autonomous: false
requirements:
  - TEST-01
  - TEST-06
  - TEST-07

must_haves:
  truths:
    - "pytest passes 100% — all new and pre-existing tests are green"
    - "Aircraft stale exclusion tests (TEST-01) pass green"
    - "Freshness unit tests (TEST-06) pass green"
    - "All existing happy-path contract tests for /api/aircraft, /api/military, /api/ships, /api/gps-jamming still pass"
  artifacts:
    - path: "backend/tests/"
      provides: "Full test suite — all green"
      contains: "test_aircraft.py test_military.py test_ships.py test_gps_jamming.py test_freshness.py"
  key_links:
    - from: "backend/tests/test_aircraft.py"
      to: "pytest full suite"
      via: "docker compose exec backend pytest tests/ -q"
      pattern: "passed"
---

<objective>
Run the full test suite and confirm 100% green — all pre-written tests (TEST-01, TEST-06) pass, and all existing happy-path contracts (TEST-07) are unbroken after the v4.0 changes.

Purpose: This is the phase gate verification. Plans 22-01 and 22-02 wrote the missing tests. This plan runs the whole suite and pauses for human confirmation that nothing is flaky or broken.
Output: A passing full test run with zero failures.
</objective>

<execution_context>
@/Users/joaoribeiro/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joaoribeiro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run targeted pre-existing test verification (TEST-01, TEST-06)</name>
  <files></files>
  <action>
Run the pre-written tests that were already green before Phase 22. Confirm each passes.

1. Run freshness unit tests (TEST-06):
   `docker compose exec backend pytest tests/test_freshness.py -v`
   Expected: 7 tests pass (6 freshness + 1 settings defaults + 1 env override = ~8 total). All must pass.

2. Run aircraft stale/fallback tests (TEST-01):
   `docker compose exec backend pytest tests/test_aircraft.py -k "excludes_stale or position_age_fallback or position_age_null" -v`
   Expected: test_list_aircraft_excludes_stale, test_list_aircraft_position_age_fallback, test_list_aircraft_position_age_null_when_both_null all pass.

If any test fails here, diagnose the failure before proceeding. The most likely causes:
- Docker stack is not running: run `docker compose up -d` then retry
- DB schema not up to date: run `docker compose exec backend alembic upgrade head` then retry
- Import error in freshness module: check backend/app/freshness.py exists
  </action>
  <verify>
    <automated>docker compose exec backend pytest tests/test_freshness.py tests/test_aircraft.py -k "stale or fallback or freshness" -q</automated>
  </verify>
  <done>All freshness unit tests and aircraft stale/fallback integration tests pass. Zero failures, zero errors.</done>
</task>

<task type="auto">
  <name>Task 2: Run full test suite (TEST-07 regression gate)</name>
  <files></files>
  <action>
Run the complete test suite to confirm every test across all files passes.

Command:
`docker compose exec backend pytest tests/ -q --tb=short`

All test files are verified:
- tests/test_aircraft.py — list, freshness, detail, stale exclusion, fallback, new fields
- tests/test_military.py — list, detail, freshness keys, shape preserved, stale exclusion (new from 22-01)
- tests/test_ships.py — list, detail, freshness keys, shape preserved, stale exclusion (new from 22-01)
- tests/test_gps_jamming.py — aggregation unit tests, route test, JAM-01/02/03 tests, source_is_stale DB test (new from 22-02)
- tests/test_freshness.py — stale_cutoff, is_stale, settings defaults, env override
- tests/test_ingest_aircraft.py — upsert unit tests
- tests/test_ingest_military.py — military ingest unit tests
- tests/test_ingest_ais.py — AIS batch flush unit tests

If a test fails with a DB connection error: the Docker stack is not running. Run `docker compose up -d` and retry.

If a test fails with a schema error (column not found): run `docker compose exec backend alembic upgrade head` to apply migrations, then retry.

If a test fails unexpectedly after both plans 22-01 and 22-02 completed successfully: report the specific test name and error output — do NOT silently suppress.
  </action>
  <verify>
    <automated>docker compose exec backend pytest tests/ -q --tb=short</automated>
  </verify>
  <done>pytest exits 0 — all tests pass, zero failures, zero errors. Output shows "passed" with no "failed" or "error" entries.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 22 full test suite: all 7 TEST requirements covered.
    - TEST-01: Aircraft stale exclusion + position_age fallback (pre-written, now verified)
    - TEST-02: geo_altitude/vertical_rate/position_source in /api/aircraft/ response (new route change + test)
    - TEST-03: Military stale/inactive exclusion (new tests in test_military.py)
    - TEST-04: Ships stale/inactive exclusion (new tests in test_ships.py)
    - TEST-05: GPS jamming source_is_stale=True DB integration (new test in test_gps_jamming.py)
    - TEST-06: freshness.py unit tests (pre-written, now verified)
    - TEST-07: All existing happy-path contracts still pass (full suite run)
  </what-built>
  <how-to-verify>
    1. Confirm Task 2 output shows 0 failures: look for "X passed" with no "failed" or "error"
    2. Confirm the three new test sections are present:
       - `docker compose exec backend pytest tests/test_military.py -k "stale or inactive" -v` — shows 2 tests pass
       - `docker compose exec backend pytest tests/test_ships.py -k "stale or inactive" -v` — shows 2 tests pass
       - `docker compose exec backend pytest tests/test_gps_jamming.py -k "source_is_stale_true_from_db" -v` — shows 1 test passes
       - `docker compose exec backend pytest tests/test_aircraft.py -k "geo_altitude" -v` — shows 1 test passes
    3. Confirm routes_aircraft.py list response includes geo_altitude: open backend/app/api/routes_aircraft.py and look for "geo_altitude" in the list_aircraft return dict (lines ~49-70)
  </how-to-verify>
  <resume-signal>Type "approved" or describe any failing tests</resume-signal>
</task>

</tasks>

<verification>
Full suite command: `docker compose exec backend pytest tests/ -q`
Expected: All tests pass, exit code 0.
</verification>

<success_criteria>
- pytest exits 0 with zero failures and zero errors
- test_freshness.py: all unit tests pass (TEST-06)
- test_aircraft.py: stale exclusion, position_age fallback, geo_altitude/vertical_rate/position_source tests pass (TEST-01, TEST-02)
- test_military.py: stale + inactive exclusion tests pass (TEST-03)
- test_ships.py: stale + inactive exclusion tests pass (TEST-04)
- test_gps_jamming.py: source_is_stale DB integration test passes (TEST-05)
- All pre-existing happy-path contract tests pass (TEST-07)
- User has confirmed via checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/22-tests/22-03-SUMMARY.md`
</output>
