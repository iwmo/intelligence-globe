---
phase: 12-osint-event-correlation
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - backend/tests/test_osint.py
  - frontend/src/components/__tests__/OsintEventPanel.test.tsx
  - frontend/src/components/__tests__/PlaybackBar.category.test.tsx
  - frontend/src/components/__tests__/SatelliteLayer.overpass.test.tsx
  - frontend/src/workers/__tests__/propagation.worker.test.ts
  - frontend/src/store/__tests__/useAppStore.test.ts
  - frontend/src/hooks/__tests__/useOsintEvents.test.ts
autonomous: true
requirements:
  - REP-05
  - REP-06

must_haves:
  truths:
    - "Every test file listed exists and fails for the right reason (missing implementation, not import/collection errors)"
    - "Backend test stubs fail with 404 or 422 matching the contract, not 500 or connection errors"
    - "Frontend stubs fail with component-not-found or undefined-function, not module resolution errors"
  artifacts:
    - path: "backend/tests/test_osint.py"
      provides: "RED tests for GET /api/osint-events and POST /api/osint-events"
    - path: "frontend/src/workers/__tests__/propagation.worker.test.ts"
      provides: "RED test for COMPUTE_OVERPASS pure elevation logic"
    - path: "frontend/src/components/__tests__/OsintEventPanel.test.tsx"
      provides: "RED smoke test for OsintEventPanel form rendering"
    - path: "frontend/src/components/__tests__/PlaybackBar.category.test.tsx"
      provides: "RED test for category chip rendering and activeCategories filtering"
    - path: "frontend/src/components/__tests__/SatelliteLayer.overpass.test.tsx"
      provides: "RED smoke test for TLE age warning in SatelliteLayer"
    - path: "frontend/src/store/__tests__/useAppStore.test.ts"
      provides: "Extended with activeCategories initial state and toggleCategory behavior"
    - path: "frontend/src/hooks/__tests__/useOsintEvents.test.ts"
      provides: "RED test for useOsintEvents hook disabled state and event array shape"
  key_links:
    - from: "backend/tests/test_osint.py"
      to: "/api/osint-events"
      via: "AsyncClient(ASGITransport(app=app))"
      pattern: "test_list_events|test_create_event|test_invalid_category"
    - from: "frontend/src/workers/__tests__/propagation.worker.test.ts"
      to: "computeOverpassElevation pure function"
      via: "direct import (not Worker postMessage)"
      pattern: "computeOverpassElevation"
---

<objective>
Create all failing test stubs for Phase 12 (RED phase). Every test must fail due to missing
implementation — not due to import/collection errors.

Purpose: Establish the test contracts that Plans 02-05 will satisfy, and prevent regressions.
Output: 7 test files, all RED, all runnable without crashes.
</objective>

<execution_context>
@/Users/joaoribeiro/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joaoribeiro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-osint-event-correlation/12-RESEARCH.md
@.planning/phases/12-osint-event-correlation/12-VALIDATION.md
</context>

<interfaces>
<!-- Key interfaces the executor needs. Extracted from codebase. -->

From frontend/src/store/useAppStore.ts (current — NO areaOfInterest or activeCategories yet):
```typescript
interface AppState {
  replayMode: 'live' | 'playback';
  replayTs: number;
  tleLastUpdated: string | null;
  layers: { satellites: boolean; aircraft: boolean; militaryAircraft: boolean; ships: boolean; gpsJamming: boolean; streetTraffic: boolean };
  // Phase 12 slices NOT YET ADDED — tests assert they do NOT exist (RED)
}
```

From frontend/src/data/osintEvents.ts (current — BLACKOUT present, SEISMIC absent):
```typescript
export interface OsintEvent {
  id: string;
  ts: number; // ms epoch
  category: 'KINETIC' | 'AIRSPACE' | 'MARITIME' | 'JAMMING' | 'BLACKOUT';
  label: string;
}
export const EVENT_COLORS: Record<string, string>;
export const OSINT_EVENTS: OsintEvent[];
```

From backend/app/api/routes_ships.py (pattern for test_osint.py):
```python
# AsyncClient(ASGITransport(app=app)) pattern established
# from httpx import AsyncClient, ASGITransport
# from app.main import app
```

From frontend/src/workers/propagation.worker.ts (current message types):
```typescript
type WorkerMessage = LoadOmmMessage | PropagateMessage | ComputeOrbitMessage | GetPositionMessage;
// COMPUTE_OVERPASS NOT YET ADDED — tests assert RED on pure function import
```
</interfaces>

<feature>
  <name>Wave 0 Test Stubs — Phase 12</name>
  <files>
    backend/tests/test_osint.py,
    frontend/src/workers/__tests__/propagation.worker.test.ts,
    frontend/src/components/__tests__/OsintEventPanel.test.tsx,
    frontend/src/components/__tests__/PlaybackBar.category.test.tsx,
    frontend/src/components/__tests__/SatelliteLayer.overpass.test.tsx,
    frontend/src/store/__tests__/useAppStore.test.ts,
    frontend/src/hooks/__tests__/useOsintEvents.test.ts
  </files>
  <behavior>
    Backend (test_osint.py):
    - test_list_events: GET /api/osint-events → expects {"events": [...]} with 200; currently returns 404 (route not registered)
    - test_create_event: POST /api/osint-events with valid body → expects response with "id" key and 200/201; currently 404
    - test_invalid_category: POST /api/osint-events with category="INVALID" → expects 422; currently 404

    Frontend worker (propagation.worker.test.ts):
    - Extract pure function `computeOverpassElevation(satrec, timestamp, observerLat, observerLon)` from worker
      - This pure function avoids Worker postMessage complexity in tests
      - RED: function does not exist yet; test imports it and asserts it returns an array
    - test_elevation_above_threshold: given a satrec at known high elevation, returns norad in result
    - test_below_threshold_excluded: given a satrec at negative elevation, not included in result

    Frontend OsintEventPanel (OsintEventPanel.test.tsx):
    - Smoke: import OsintEventPanel from components/OsintEventPanel — RED: component does not exist
    - Assert renders form with label input, datetime input, category select, source URL input

    Frontend PlaybackBar.category.test.tsx:
    - Mock useAppStore to return activeCategories: [] and toggleCategory fn
    - Assert category chips (KINETIC, AIRSPACE, MARITIME, SEISMIC, JAMMING) render when replayMode='playback'
    - Assert clicking a chip calls toggleCategory with correct string
    - RED: activeCategories not in store, chips not in PlaybackBar

    Frontend SatelliteLayer.overpass.test.tsx:
    - Smoke: when tleLastUpdated is 8 days ago and replayMode is 'playback', a TLE staleness warning element is rendered
    - RED: warning element does not exist in current SatelliteLayer

    Frontend useAppStore.test.ts (EXTEND existing file):
    - Add tests for: activeCategories defaults to []
    - Add tests for: toggleCategory('KINETIC') adds 'KINETIC' to activeCategories
    - Add tests for: toggleCategory('KINETIC') again removes it
    - Add tests for: areaOfInterest defaults to null
    - RED: these fields do not exist in the store yet

    Frontend useOsintEvents.test.ts:
    - Smoke: import useOsintEvents from hooks/useOsintEvents — RED: hook does not exist
    - Assert hook returns { events: OsintEvent[], isLoading: boolean }
    - Assert in disabled=true state hook does not fetch (no network calls)
  </behavior>
  <implementation>
    Write each test file to assert the correct contract. Use deferred import pattern for
    backend tests (ModuleNotFoundError = correct RED signal). Use vi.mock for Cesium and
    react-query in frontend tests following Phase 08/09 established patterns.

    CRITICAL — propagation.worker.test.ts strategy: Do NOT test via Worker postMessage
    (untestable in vitest/jsdom). Instead, the test imports a PURE FUNCTION
    `computeOverpassElevation` that will be extracted to a separate file
    `frontend/src/workers/overpassElevation.ts` in Plan 03. The test stubs import from
    that path — it RED-fails now because the file does not exist.

    CRITICAL — useAppStore.test.ts: Read the existing file first. APPEND new describe blocks
    for Phase 12 slices. Do not overwrite existing passing tests.

    All vi.mock calls must be at module top level (static import + vi.mock hoisting pattern).
    Use `@testing-library/react` renderHook for hook tests.
  </implementation>
</feature>

<verification>
<automated>
cd backend && python -m pytest tests/test_osint.py -x -q 2>&1 | tail -5
</automated>
<automated>
cd frontend && npx vitest run src/workers/__tests__/propagation.worker.test.ts src/components/__tests__/OsintEventPanel.test.tsx src/components/__tests__/PlaybackBar.category.test.tsx src/components/__tests__/SatelliteLayer.overpass.test.tsx src/hooks/__tests__/useOsintEvents.test.ts 2>&1 | tail -10
</automated>
<automated>
cd frontend && npx vitest run src/store/__tests__/useAppStore.test.ts 2>&1 | tail -5
</automated>
All test runs must exit non-zero (RED) or show "failed" — never an uncaught collection error.
Existing tests in useAppStore.test.ts must remain passing.
</verification>

<success_criteria>
- All 7 test files exist and are syntactically valid
- Backend pytest collects 3 tests in test_osint.py; all fail with 404 (route not registered)
- Frontend vitest collects all stubs without import/collection errors
- New useAppStore tests fail with "activeCategories is not defined" or similar assertion errors
- Existing useAppStore tests continue to pass
- cd frontend && npx vitest run src/store/__tests__/useAppStore.test.ts shows some passing + some failing
</success_criteria>

<output>
After completion, create `.planning/phases/12-osint-event-correlation/12-01-SUMMARY.md`
</output>
