---
phase: 40-v10-tech-debt-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/hooks/useAircraft.ts
autonomous: true
requirements:
  - CLEANUP-02
must_haves:
  truths:
    - "useAircraft.ts staleTime is 15000 (15 seconds), not 90000"
    - "useAircraft.ts refetchInterval is 15000 when replayMode is live, not 90000"
    - "No mention of OpenSky in useAircraft.ts comments"
  artifacts:
    - path: "frontend/src/hooks/useAircraft.ts"
      provides: "Aircraft query hook with ADSB.lol-aligned poll cadence"
      contains: "staleTime: 15_000"
  key_links:
    - from: "frontend/src/hooks/useAircraft.ts"
      to: "staleTime / refetchInterval"
      via: "useQuery options"
      pattern: "15_000"
---

<objective>
Update `useAircraft.ts` to align its poll interval with the ADSB.lol backend cadence (15s instead of OpenSky's 90s) and remove the stale OpenSky comment.

Purpose: The hook still carries OpenSky's 90-second polling logic and an explanatory comment referencing OpenSky. ADSB.lol data refreshes approximately every 15 seconds — keeping 90s means the globe shows up to 90-second-old data when ADSB.lol delivers fresh data every 15s.
Output: `useAircraft.ts` with `staleTime: 15_000` and `refetchInterval: 15_000` and no OpenSky comment.
</objective>

<execution_context>
@/Users/joaoribeiro/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joaoribeiro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@frontend/src/hooks/useAircraft.ts
</context>

<interfaces>
<!-- Current useAircraft.ts lines to change (lines 50-51): -->
```typescript
// Current (stale):
staleTime: 90_000,         // 90 seconds — matches OpenSky poll interval
refetchInterval: replayMode === 'live' ? 90_000 : false,

// Target:
staleTime: 15_000,         // 15 seconds — matches ADSB.lol backend cadence
refetchInterval: replayMode === 'live' ? 15_000 : false,
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Update poll interval and remove OpenSky comment</name>
  <files>frontend/src/hooks/useAircraft.ts</files>
  <action>
    Make exactly two changes to `frontend/src/hooks/useAircraft.ts`:

    1. Line 50: Replace `staleTime: 90_000,         // 90 seconds — matches OpenSky poll interval`
       with:     `staleTime: 15_000,         // 15 seconds — matches ADSB.lol backend cadence`

    2. Line 51: Replace `refetchInterval: replayMode === 'live' ? 90_000 : false,`
       with:     `refetchInterval: replayMode === 'live' ? 15_000 : false,`

    No other lines change. Do NOT touch the AircraftRecord interface, the queryFn, the abort controller timeout, the staleTime comment on line 28 about the query key, or any other logic.
  </action>
  <verify>
    <automated>grep -n "staleTime\|refetchInterval\|OpenSky" frontend/src/hooks/useAircraft.ts</automated>
  </verify>
  <done>
    - `grep staleTime frontend/src/hooks/useAircraft.ts` shows `15_000`, not `90_000`
    - `grep refetchInterval frontend/src/hooks/useAircraft.ts` shows `15_000`, not `90_000`
    - `grep OpenSky frontend/src/hooks/useAircraft.ts` returns no results (comment removed)
    - TypeScript compiles: `cd frontend && npx tsc --noEmit` passes
  </done>
</task>

</tasks>

<verification>
- `grep -n "90_000\|OpenSky" frontend/src/hooks/useAircraft.ts` returns no matches
- `grep -n "15_000" frontend/src/hooks/useAircraft.ts` matches both staleTime and refetchInterval lines
- `cd frontend && npx tsc --noEmit` passes without errors
</verification>

<success_criteria>
`useAircraft.ts` polls at 15s cadence matching ADSB.lol. No stale OpenSky reference remains in the file.
</success_criteria>

<output>
After completion, create `.planning/phases/40-v10-tech-debt-cleanup/40-02-SUMMARY.md`
</output>
