---
phase: 40-v10-tech-debt-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/workers/ingest_aircraft.py
autonomous: true
requirements:
  - CLEANUP-01
must_haves:
  truths:
    - "backend/app/workers/ingest_aircraft.py does not exist on disk"
    - "No OpenSky-era dead code remains in the workers directory"
  artifacts:
    - path: "backend/app/workers/ingest_adsbiol.py"
      provides: "Active ADSB.lol ingest worker (pre-existing, untouched)"
    - path: "backend/app/workers/ingest_ais.py"
      provides: "Active AIS ingest worker (pre-existing, untouched)"
  key_links:
    - from: "backend/app/workers/"
      to: "ingest_aircraft.py"
      via: "file deletion"
      pattern: "file must NOT exist after deletion"
---

<objective>
Delete `backend/app/workers/ingest_aircraft.py` — the dead OpenSky-era commercial aircraft ingest worker superseded by `ingest_adsbiol.py` in Phase 38.

Purpose: The file is dead code; it references OpenSky OAuth2 APIs and credit-budget logic that no longer exists. Leaving it in the workers directory is misleading and risks accidental re-enabling.
Output: File deleted from disk and removed from git tracking.
</objective>

<execution_context>
@/Users/joaoribeiro/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joaoribeiro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete ingest_aircraft.py</name>
  <files>backend/app/workers/ingest_aircraft.py</files>
  <action>
    Delete the file `backend/app/workers/ingest_aircraft.py` using `git rm backend/app/workers/ingest_aircraft.py`. This removes it from both the filesystem and git tracking in a single step.

    Do NOT modify any other file. The replacement worker `ingest_adsbiol.py` already exists and is wired into `worker.py` — no wiring changes are needed.

    Verify the workers directory still contains `__init__.py`, `ingest_adsbiol.py`, and `ingest_ais.py` after deletion.
  </action>
  <verify>
    <automated>test ! -f backend/app/workers/ingest_aircraft.py && echo "DELETED" || echo "STILL EXISTS"</automated>
  </verify>
  <done>`backend/app/workers/ingest_aircraft.py` does not exist. `git status` shows it as deleted. `ingest_adsbiol.py` and `ingest_ais.py` remain untouched.</done>
</task>

</tasks>

<verification>
- `ls backend/app/workers/` confirms: `__init__.py`, `ingest_adsbiol.py`, `ingest_ais.py` — no `ingest_aircraft.py`
- `git status` shows `D backend/app/workers/ingest_aircraft.py`
- Backend tests still pass: `cd backend && python -m pytest tests/ -x -q` (worker tests unaffected since test_ingest_aircraft.py was retired with pytest.skip in Phase 38)
</verification>

<success_criteria>
`ingest_aircraft.py` is deleted. No OpenSky-era worker files remain in `backend/app/workers/`.
</success_criteria>

<output>
After completion, create `.planning/phases/40-v10-tech-debt-cleanup/40-01-SUMMARY.md`
</output>
