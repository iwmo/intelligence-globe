---
phase: 21-api-route-filtering
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - backend/tests/test_military.py
  - backend/tests/test_ships.py
  - backend/tests/test_gps_jamming.py
autonomous: true
requirements: [MIL-02, SHIP-02, JAM-02, JAM-03]

must_haves:
  truths:
    - "Failing tests exist for MIL-02: active+fresh filter, fetched_at and is_stale in response"
    - "Failing tests exist for SHIP-02: active+fresh filter, last_seen_at/fetched_at/is_stale in response"
    - "Failing tests exist for JAM-02: envelope includes aggregated_at, source_fetched_at, source_is_stale"
    - "Failing test exists for JAM-03: stale cells returned with source_is_stale=true (not empty set)"
    - "Existing tests (test_list_military, test_list_ships, test_gps_jamming_route) still pass"
  artifacts:
    - path: "backend/tests/test_military.py"
      provides: "MIL-02 failing tests appended"
    - path: "backend/tests/test_ships.py"
      provides: "SHIP-02 failing tests appended"
    - path: "backend/tests/test_gps_jamming.py"
      provides: "JAM-02 and JAM-03 failing tests appended"
  key_links:
    - from: "tests/test_military.py"
      to: "GET /api/military/"
      via: "AsyncClient ASGI transport"
      pattern: "client.get.*api/military"
    - from: "tests/test_gps_jamming.py"
      to: "GET /api/gps-jamming"
      via: "AsyncClient ASGI transport"
      pattern: "client.get.*api/gps-jamming"
---

<objective>
Write failing (RED) tests for all route-layer requirements in Phase 21 before touching any route code.

Purpose: Establish the test contract before implementation. Tests must fail RED now, then turn GREEN when Plans 02 and 03 implement the routes.
Output: Three test files with new failing test functions appended — existing tests are left untouched.
</objective>

<execution_context>
@/Users/joaoribeiro/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joaoribeiro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@backend/tests/test_military.py
@backend/tests/test_ships.py
@backend/tests/test_gps_jamming.py
@backend/app/api/routes_military.py
@backend/app/api/routes_ships.py
@backend/app/api/routes_gps_jamming.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing tests for MIL-02 and SHIP-02</name>
  <files>backend/tests/test_military.py, backend/tests/test_ships.py</files>
  <behavior>
    MIL-02 tests (append to test_military.py):
    - test_military_response_includes_freshness_keys: GET /api/military/ returns 200, body is list; when rows are present each item has "fetched_at" and "is_stale" keys — assert both keys present in each item (fails RED now because route does not return them yet)
    - test_military_list_shape_preserved: All existing keys still present — hex, flight, aircraft_type, alt_baro, gs, track, lat, lon, squawk, updated_at — assert each key present (passes RED because route currently returns them; guards regression)

    SHIP-02 tests (append to test_ships.py):
    - test_ships_response_includes_freshness_keys: GET /api/ships/ returns 200, body is list; when rows are present each item has "last_seen_at", "fetched_at", and "is_stale" keys (fails RED now)
    - test_ships_list_shape_preserved: All existing keys still present — mmsi, vessel_name, vessel_type, lat, lon, sog, cog, heading, nav_status, last_update, updated_at (passes RED; guards regression)

    Note: These integration tests use AsyncClient with ASGITransport(app=app) and base_url="http://test", same pattern as existing tests in each file. Tests that assert on new keys will fail RED because the routes do not yet return fetched_at, is_stale, last_seen_at.
  </behavior>
  <action>
    Append to backend/tests/test_military.py — add two new test functions after the existing tests. Use same AsyncClient pattern as test_list_military. The test_military_response_includes_freshness_keys test iterates body items and asserts "fetched_at" in item and "is_stale" in item. The test_military_list_shape_preserved test asserts hex, flight, aircraft_type, alt_baro, gs, track, lat, lon, squawk, updated_at are present in each item.

    Append to backend/tests/test_ships.py — add two new test functions after the existing tests. The test_ships_response_includes_freshness_keys test iterates body items and asserts "last_seen_at" in item, "fetched_at" in item, "is_stale" in item. The test_ships_list_shape_preserved test asserts mmsi, vessel_name, vessel_type, lat, lon, sog, cog, heading, nav_status, last_update, updated_at are all present.

    Both test files must retain all existing test functions unchanged.
  </action>
  <verify>
    <automated>cd "/Users/joaoribeiro/Desktop/INTELLIGENCE GLOBE/backend" && python -m pytest tests/test_military.py tests/test_ships.py -x -q 2>&1 | tail -20</automated>
  </verify>
  <done>
    New tests collected, existing tests pass, new freshness-key tests fail (FAILED — KeyError or AssertionError on missing key). Zero import errors. Commit: test(21-01): add failing MIL-02 and SHIP-02 tests
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write failing tests for JAM-02 and JAM-03</name>
  <files>backend/tests/test_gps_jamming.py</files>
  <behavior>
    JAM-02 tests (append to test_gps_jamming.py):
    - test_gps_jamming_envelope_includes_metadata_keys: GET /api/gps-jamming returns 200; body has "aggregated_at", "source_fetched_at", "source_is_stale" keys at the top level alongside "cells" (fails RED now — route currently returns only {"cells": [...]})

    JAM-03 test (append to test_gps_jamming.py):
    - test_gps_jamming_source_is_stale_present_in_envelope: GET /api/gps-jamming returns 200; assert "source_is_stale" in body (key present even if value is None when table is empty). Add docstring: "JAM-03: source_is_stale key must always be present in response envelope — stale cells are returned with source_is_stale=true rather than silently dropping cells."

    These tests use the same AsyncClient pattern as existing test_gps_jamming_route. They assert key presence on the top-level response dict. They fail RED now because the route only returns {"cells": [...]}.
  </behavior>
  <action>
    Append to backend/tests/test_gps_jamming.py — add a new section after the existing JAM-01 tests. The two new test functions (test_gps_jamming_envelope_includes_metadata_keys and test_gps_jamming_source_is_stale_present_in_envelope) use AsyncClient with ASGITransport(app=app) exactly as test_gps_jamming_route does. The first asserts all three metadata keys are present in body. The second specifically asserts "source_is_stale" in body with a JAM-03 docstring explaining the non-empty-set contract.

    All existing test functions are preserved unchanged.
  </action>
  <verify>
    <automated>cd "/Users/joaoribeiro/Desktop/INTELLIGENCE GLOBE/backend" && python -m pytest tests/test_gps_jamming.py -x -q 2>&1 | tail -20</automated>
  </verify>
  <done>
    New JAM-02 and JAM-03 tests fail RED (AssertionError — "aggregated_at" not in body). All pre-existing GPS jamming tests still pass. Zero import errors. Commit: test(21-01): add failing JAM-02 and JAM-03 tests
  </done>
</task>

</tasks>

<verification>
Full test suite wave check:

```
cd "/Users/joaoribeiro/Desktop/INTELLIGENCE GLOBE/backend" && python -m pytest tests/test_military.py tests/test_ships.py tests/test_gps_jamming.py -v 2>&1 | tail -30
```

Expected state:
- Existing tests (test_list_military, test_military_detail, test_list_ships, test_ship_detail, test_gps_jamming_route, all JAM-01 tests): PASSED
- New freshness-key tests: FAILED (RED — expected)
- Zero collection errors
</verification>

<success_criteria>
- 4 new failing tests added across 3 files, all with correct assertions
- No existing tests broken
- All new tests fail for the right reason (key missing, not import error)
- Committed as two atomic commits with test(21-01): prefix
</success_criteria>

<output>
After completion, create `.planning/phases/21-api-route-filtering/21-01-SUMMARY.md`
</output>
